# 돌파 엔진 정리 (잠금/유지 이유) — 2026-07-04

> 「돌파사냥꾼(breakout_unified_v1)」 = 신고가+베이스 2모드 통합 신설. 아래는 기존 13개 처리 + 이유.

## 🚀 새 통합엔진 (신설·★실탄)
| 엔진 | 역할 |
|---|---|
| **`breakout_unified_v1.py` 「돌파사냥꾼」** | 신고가🅰 + 베이스🅱 2모드 1엔진. **★실탄 ON(`BRKUNI_LIVE=YES`)**. 태스크 `SAFEPLUS_BRKUNI_SHADOW`(env로 실탄). 안전=CAP30·TOP2·che≥100필수·전역상한·중복배제. 상한가→익일시가매도. 킬스위치 `setx BRKUNI_LIVE NO`. **BRK/MBB 대체함** |

> 2026-07-04 실탄 전환: 친구님 "실전용 만들어 그림자 하지마". 베이스모드 조임(박스≤3%+거래량)으로 과다발동 제거. BRK/MBB는 아래 잠금.

## 🔒 잠금(Disabled) — 이유
| 엔진 / 태스크 | 잠근 이유 |
|---|---|
| `breakout_shadow` / `SAFEPLUS_BREAKOUT_SHADOW` | 신고가 돌파 **관찰판**. 돌파사냥꾼 현황판이 대체 → 중복 |
| `morning_base_breakout_shadow` / `SAFEPLUS_MBB_SITUATION` | 베이스 돌파 **관찰판**. 돌파사냥꾼이 대체 → 중복 |
| `brk_realtime_breakout_shadow` / `SAFEPLUS_BRK_RT_SHADOW` | 실시간 신고가 **실험판**. 돌파사냥꾼 신고가모드로 흡수 |
| `early_momentum_break_shadow` / `SAFEPLUS_EARLY_MOMENTUM` | 조기 모멘텀(신고가의 빠른 변형). 안 쓰던 것(이미 Disabled) |
| `ignition_shadow` / `SAFEPLUS_IGNITION_SHADOW` | 점화 급등(+5%) 퍼널. 안 쓰던 것(이미 Disabled) |

## ✅ 유지 — 이유 (잠그면 안 됨)
| 엔진 / 태스크 | 유지 이유 |
|---|---|
| ~~`breakout_live_executor`~~ | ★**잠금(`BRK_LIVE=NO`)** — 돌파사냥꾼 신고가모드가 대체. 태스크는 남기되 주문0(중복매매 차단) |
| ~~`morning_base_breakout_executor`~~ | ★**잠금(`MBB_LIVE=NO`)** — 돌파사냥꾼 베이스모드가 대체. 태스크는 남기되 주문0 |
| `base_thrust_setup_screener` / `SAFEPLUS_BASE_THRUST_SETUP` | ★**BRK 실행기에 셋업존 종목 공급(feeder)** — 잠그면 BRK 입력 끊김 |
| `base_surge_shadow`·`base_surge_intraday` / `SAFEPLUS_BASE_SURGE_*` | 베이스 갑툭 후보 CSV 발행(소비처 확인 전 유지) |
| `coil_breakout_shadow` / `SAFEPLUS_COIL_*` | 코일 수렴 감시(소비처 확인 전 유지) |
| `newhigh_breakout_scanner` / `SAFEPLUS_NEWHIGH_SCANNER` | 매매 아님·**micro_watch 종목공급(feeder)** — 유지 |

## 다음 단계
- 돌파사냥꾼 그림자 검증 OK → `BRKUNI_LIVE=YES` 실탄 전환 →
  그때 **BRK/MBB(및 feeder base_thrust) 잠금** = 돌파도 "핵심 1개"로 완결.
- base_surge·coil 소비처 확인되면 추가 잠금 검토.
