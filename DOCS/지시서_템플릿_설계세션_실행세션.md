# 지시서 템플릿 — 설계 세션(파블) → 실행 세션(오퍼스/소넷)

> 용도: 파블 세션이 설계하고, 다른 모델 세션이 실행하는 이원 체제 (CLAUDE.md K항).
> 사용법: 파블이 이 틀로 지시서를 `보고서\지시서_<주제>_<날짜>.md`에 쓰면,
> 실행 세션(C:\stock_bot에서 열기 — CLAUDE.md·메모리 자동 적재)에 "이 파일 실행해"만 전달.
> 기존 검증 사례: 보고서\*_지시서_GPT_*.md (2026-08-10~14 수리들).

---

## 1. 목표 (결과)
- 완료되면 무엇이 참이 되는가. 검증 가능한 문장으로 1~3줄.

## 2. 왜 (이유)
- 이 작업이 필요한 근거·사고 맥락. 실행자가 재량을 올바르게 쓰게 하는 핵심.

## 3. 제약 (반드시 지킬 것)
- 수정 허용 파일 목록 (그 외 수정 금지 — 발견 문제는 한 줄 보고+안건화만)
- 편집 전 백업: `RUN\backup\<파일명>_<날짜>_before_<수리이름>.py` + 원본 mtime 기록
- 장중(08:40~15:30) 실전 파일 편집 금지 · .cmd=CRLF · .ps1/.cmd=ASCII · PowerShell로 JSON 금지
- 승인 명부는 `RUN\approval_manifest_writer_v1.py`(CAS)로만 · 관리자 기준표 재기준선은 친구님 직접
- 루프 규율: 시도당 수정 1개 · 최대 3회 · 실패 시 백업으로 원상복구 후 보고

## 4. 절차 (순서 고정)
1. [행동] → 완료 조건: [검증 가능한 상태]
2. ...
- 독립 단계는 병렬 가능 여부 명시.

## 5. 완료 증거 (자가보고 불신 — 이것 없이 "완료" 선언 금지)
- diff(백업 vs 현행) 요약 · 컴파일/테스트 실행 원문 출력 · 관문/명부 검증 출력
- 전체 pytest 실패 개수 전후 대조 (신규 실패 0 증명; 2026-08-19 기준선 = 38 failed)

## 6. 실패 대응표
| 증상 | 1차 시도 | 그래도 실패 시 |
|---|---|---|
| (예) 테스트 신규 실패 | 원인 1개 수정 후 재실행 | 원상복구 + 시도 기록 보고 |

## 7. 보고 형식
- 결론 먼저 · 전부 한글 · 사실마다 [실측]/[기억]/[추정] 태그 · 증거 파일 경로 첨부
