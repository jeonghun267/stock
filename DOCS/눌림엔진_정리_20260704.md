# 눌림 엔진 정리 (하나만 남기고 잠금·이유) — 2026-07-04

> 친구님: "작은 눌림에도 수없이 생겨 눌림을 제대로 못 해봤다 → 제대로 만들고, 하나만 남기고 다 잠가라(이유 적고)."
> 결과: **「눌림사냥꾼」 1개만 실탄, 나머지 11개(+딥바운스) 전부 잠금.** (진짜 열린 포지션 0개 확인 후 안전하게 잠금)

## 🎯 남긴 하나 (실탄)
| 엔진 | 역할 |
|---|---|
| **`pullback_unified_v1.py` 「눌림사냥꾼」** | ★**실탄(`PBUNI_LIVE=YES`)**. 진짜 눌림목만 선별 = 정배열+당일상승≥5%+되돌림3~8%+5선까지눌림+5선양봉회복+**거래량 재유입≥1.6배**+che≥100. 매도=추세용(che끌기+20선+상한가익일시가). 태스크 `SAFEPLUS_PBUNI_SHADOW`(env로 실탄). TOP6·전략100·소액캡. 킬 `setx PBUNI_LIVE NO` |

## 🔒 잠근 것 + 왜

### 상승중 눌림 (눌림사냥꾼이 대체) — 중복
| 엔진 / 처리 | 잠근 이유 |
|---|---|
| **NEW_PB** (`new_pullback_live_executor`) · `NEW_PB_LIVE=NO` + 태스크 PICK/SELL Disabled | 장대양봉 첫눌림 = **눌림사냥꾼과 같은 "상승중 눌림"**. 근데 기계화 시 ~+2%로 약했고(작은 눌림 과다). 눌림사냥꾼(거래량 재유입 선별)이 대체 |
| **원조 추세눌림** (`rt_execution`/`rt_risk`/`rt_sell`/`pullback_sell`/SIGA) | 5일선·기관동행 눌림. **이미 SIGA/rt 태스크 다 Disabled**(안 돌고 있었음). 눌림사냥꾼이 대체 |
| **gap_leader_pullback** (`GAPLEAD_LIVE=NO`) | 15% 갭대장 첫눌림 = 눌림사냥꾼의 특수케이스. 흡수 |
| `new_pullback_shadow` 태스크 Disabled | NEW_PB 탐지 그림자(관찰판). 눌림사냥꾼 현황판이 대체 (파일은 유지 — NEW_PB가 import) |

### 딥바운스 (이건 눌림 아니라 반전 = 바닥사냥꾼 영역)
| 엔진 / 처리 | 잠근 이유 |
|---|---|
| **deep_v** (`DEEPV_LIVE=NO` + DEEP_V_LIVE/MORNING Disabled) | 급락 -6% 후 **바닥반등** = "위 눌림"이 아니라 "아래 급락 딥". **바닥사냥꾼(반전)이 담당** |
| **gapv_dipbounce** (`GAPVRT_LIVE=NO` + GAPV_RT_SHADOW Disabled) | deep_v의 15초 스냅샷판. 동일 딥바운스 → 바닥사냥꾼 영역 |

## ✅ 유지 (매매 아님·데이터/분석)
- `new_pullback_shadow.py` **파일**(NEW_PB executor가 import) — 파일은 삭제금지, 태스크만 Disabled
- 눌림 gate/score/track/verify/backfill 유틸 ~25개(분석용) · `NEW_PULLBACK_BACKFILL`(데이터 백필)

## 안전 확인
- **잠그기 전 진짜 열린 포지션 = 0개** (rt_open 0 · new_pb 17개·deepv 1개는 전부 status=CLOSED·오래된 로그). → 실탄 꼬임 없이 안전하게 잠금.

## 결과 = 3형제 완성
- 🎯 **바닥사냥꾼**(반전·딥바운스) · 🚀 **돌파사냥꾼**(신고가·베이스) · **눌림사냥꾼**(상승중 눌림) — 각 1개 실탄, 옛 중복 다 잠금.
