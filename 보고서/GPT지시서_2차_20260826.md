# GPT 2차 수리 지시서 — 2026-08-26 (장 마감 후)

작성: Claude / 15:15
1차 지시서: `보고서\GPT지시서_S06_20260826.md` (배경·제약 상세는 그쪽 참조)

---

## ✅ 이미 끝난 것 (Claude 가 실측 검증 완료 — 다시 하지 말 것)

| 항목 | 검증 결과 |
|---|---|
| S01 미검증 STRONG_FLOW 조건 되돌림 | 잔존 0건, 69,810 → 65,374바이트 |
| S06 BOARD 유니버스 + chase_cap 2.5 복구 | 런처에 `S06_UNIVERSE_MODE=BOARD`·`S06_CHASE_CAP_PCT=2.5` 확인 |
| S06 해시 재봉인 | 14:58:15 `codex_s06_chase_cap_2p5_verified_20260826` |
| 전체 승인 해시 | **63개 전부 일치** (common 18·S01 11·S02 7·S03 14·S06 13), `MANIFEST_SHA_OK` |
| PROD_REPLAY | `reports\verified_replay\20260824\s06_chase_cap_delta_replay_v1.json` = PASS |
| .cmd 줄끝 | CRLF 45 / 순수LF 0 / 비ASCII 0 |

**⇒ 이 6건은 건드리지 말 것. 아래 3건만 하면 된다.**

---

# 🔴 수리 1 (최우선) — 주문이 나갔는데 엔진은 "실패"로 안다

## 증상 (오늘 실측 2건)

```
[2026-08-26 10:33:13] BUY_REJECTED  네이처셀(007390) 22,950
    REJECTED: SENDORDER_REAL account verification unavailable: no login account
    → 그 뒤 24,350원까지 상승. 놓친 이익 +6.10%  ★오늘 최대 기회

[2026-08-26 11:05:50] SELL_REJECTED 코오롱티슈진(950160)
    같은 오류
[2026-08-26 11:05:50] broker_journal: order_no=0348763 state=체결 qty=1 remain=0 -매도
    → 엔진은 "거부", 브로커는 "체결". 상태 불일치.
       이번엔 손익이 맞았지만 재시도 로직이 돌았다면 이중 매도가 났을 자리다.
```

## 원인 위치 — **아직 그대로다 (15:12 확인)**

`RUN\broker_gateway_v1.py` **3318~3337행**

```python
3318  try:
3319      raw_accounts = str(self.ocx.dynamicCall("GetLoginInfo(QString)", "ACCNO")).strip()
3322      logged_accounts = {item.strip().replace("-","") for item in raw_accounts.split(";") if item.strip()}
3326  except Exception as exc:
3329      error=f"SENDORDER_REAL account verification unavailable: {exc}"
3331      return
3332  if not logged_accounts:          # ★ 여기 — 단 한 번의 빈 응답으로 주문 포기
3335      error="SENDORDER_REAL account verification unavailable: no login account"
3337      return
```

**키움 OCX 가 계좌번호를 빈 문자열로 돌려주면 재시도 없이 즉시 거부한다.**
브로커 자체는 정상이었다: `state=CONNECTED`, PID 17768, 08:35부터 8,880초 무중단, TR 1,416건 처리.

## 발생 조건 (상관 실측)

```
10:15  S06 프로세스만 재시작 (브로커는 안 건드림)  →  10:33 오류 (18분 후)
11:01  S06 프로세스만 재시작                      →  11:05 오류 ( 4분 후)
09:00~10:15 (재시작 전) 구간: 0건
```
인과는 미확정. **브로커를 재기동한 적이 없는데 왜 OCX 계좌 조회가 비었는지** 밝히는 것이 핵심.

## 요구 결과

1. **원인 규명**
   - `LOG\broker_journal.log` 의 11:05:50 전후 60초, `IPC\requests\`·`IPC\responses\` 의 해당 request_id
   - **11:05:50 에 체결된 `order_no=0348763` 이 누가 낸 주문인지** 확정할 것
     (S06 매도가 거부됐는데 체결이 있었다 — 지연 통보인지 실제로 나간 것인지)
2. **재시도 도입** — 빈 응답 시 즉시 실패시키지 말고 짧은 재조회(예: 0.2초 간격 3회) 후에도
   비어 있을 때만 거부한다.
   - ⚠️ **3338행의 `account mismatch` 검사는 그대로 유지할 것.**
     이번 수리는 "빈 응답 재시도" 추가이지 계좌 검증 완화가 아니다.
3. **거부/실물 불일치 감지** — 주문이 ERROR 로 끝났는데 같은 종목·같은 방향 체결 이벤트가
   직후 도착하면 경고 로그를 남긴다. (재주문 판단은 전략 몫이므로 여기서는 기록만)

## 완료 증거
- 수정 파일 / 변경 줄
- 단위 테스트 1개: 빈 응답 → 재조회 → 성공 / 3회 모두 빈 값 → 거부
- `tests\test_broker_*` 실행 결과 (수리 전 개수와 대조)
- 해시 재봉인 (`broker_gateway_v1.py` 는 common 명부 소속)

---

# 🟠 수리 2 — S01 ROCKET 이 왜 안 뜨는지 알 수 없다

## 증상
8/25 owner 승인으로 8/26 이 ROCKET 첫 실전일이었으나 **발동 0건**.
S01 은 오늘 6건 매수했고 **전부 기존 `STRONG_FLOW` 레인**, 실현 −7.264%p(6전 1승).

## 원인 (8/26 실측)

```
오늘 S01 신호 130건 (data\audit\s01_open_priority\20260826.jsonl)
    stage 분포        {'None': 130}          ← v3 stage 가 하나도 안 채워짐
    entry_stage 분포  {'STRONG_FLOW': 130}   ← 전부 레거시 레인
```

`RUN\strategy_01_open_surge_signal_v2.py` **1311행 (15:12 확인, 그대로)**
```python
1305  eligible = [row for row in entry_v3_rows
1307      if str(row.get("stage")) == "ROCKET"      ← stage 가 None 이라 영원히 불일치
1308      and action == BUY_READY and mode == "SIGNAL_ONLY_ORDER_ZERO"]
1311  if not eligible:
1312      return None      ← 조용히 종료. 왜 0건인지 아무 기록이 없다
```

관문 2개(`S01_ROCKET_LIVE=YES`, `live_features.S01_ROCKET=true`)는 열려 있는데
**v3 엔진이 후보를 안 만들어 승격 대상이 0.**

## 요구 결과

1. **`return None` 자리에 탈락 사유를 남길 것.**
   최소 항목: `entry_v3_rows` 개수 · stage 분포 · `rocket_live` 여부 · `S01_ENTRY_V3_MODE` 값
   ⚠️ 신호당 1회만 기록(1초 틱 폭주 방지). 8/19 S03 사고 때 쓴 방식과 같게.
   이유: **지금은 "안 떴다"는 사실만 있고 "왜"가 없다.** 내일도 같은 상태면 원인을 못 찾는다.
2. **1311행 외에 1257행에도 `if not eligible:` 이 있다.** 같은 문제인지 확인하고,
   같으면 함께 처리한다.
3. ⛔ **`S01_ENTRY_V3_MODE` 를 활성화하지 말 것.**
   승인 명부에 *"remains SHADOW until current-path PROD_REPLAY passes"* 로 되어 있어
   ROCKET LIVE 승인과 **명부 자체가 모순**이다. 이 모순 해소는 친구님 판단 사항.
   **모순 사실만 보고하고 손대지 말 것.**

## 완료 증거
- 수정 파일 / 변경 줄
- 탈락 사유가 실제로 기록되는지 단위 테스트 1개
- `tests\test_strategy_01_*` 실행 결과 (수리 전 개수와 대조)
- 해시 재봉인 (S01 명부 소속)

---

# 🟡 수리 3 — 가격 하한 1만원의 기회비용 (⛔ **조사만. 변경 금지**)

## 증상 — 오늘 S06 는 5번 매수 준비를 냈는데 1번만 샀다

```
09:10:11  차이커뮤니케이션  2,610원 → PRICE_BELOW_MIN 차단
09:44:02  코오롱티슈진   18,610원 → ✅ 매수. +1.235% 실현
09:54:09  에이에프더블류    265원 → PRICE_BELOW_MIN 차단
10:07:42  원풍물산         638원 → PRICE_BELOW_MIN 차단
10:33:12  네이처셀      22,950원 → 수리 1 의 계좌 오류로 거부 (+6.10% 상실)
```

**선별 자체는 정상이다** — 5건 전부 `dip_buy_sell_ratio` 1.38~4.78(저점 매수 우위),
`flow_flip=O`, 반등 1.59~1.95%. 통과율 5/37 = 13.5%로 충분히 엄격하다.
**⇒ 매수 조건·문턱·지표를 조정할 근거는 없다. 건드리지 말 것.**

## 코드 위치
```
RUN\strategy_06_crash_low_chase_v1.py
  220행   min_price_krw = int(os.environ.get("S06_MIN_PRICE_KRW", "10000"))
 1570행   if price < self.config.min_price_krw:
 1572행       reason="PRICE_BELOW_MIN"
```

## 요구 결과 — **표만 만들 것. 코드·설정 변경 절대 금지**

오늘(8/26) 급락 38종목 전수에 대해:

1. 1만원 미만으로 차단된 종목들의 **종가까지 실제 궤적**
2. **S06 실제 진입 규칙을 그대로 적용한 가상 진입가** 기준 손익
   (⚠️ 저점 대비가 아니다. S06 는 1차반등+눌림+재반등 확인 후 진입하므로
    실제 진입가는 저점보다 2~3% 위다. 저점 기준으로 계산하면 과대평가된다)
3. 저가주의 **호가 단위 기준 왕복비용**과 유동성 위험
   (오늘 참고치: 시그네틱스 0.23% · 케이바이오랩스 0.11% · 파인테크닉스 0.41%)
4. 가능하면 최근 5거래일로 같은 표

⚠️ 오늘 하루 표본이고, **오늘은 저가주가 유독 무너진 날**이라 저가주에 유리하게 나올 수 있다.
그 편향을 표에 명시할 것.

**1만원 하한은 친구님이 정하신 규칙이다. 판단 재료만 만들고 변경 제안은 표 안에만 적을 것.**

---

## ⛔ 하면 안 되는 것 (재확인)

- 매수·매도 **조건/문턱/수량/슬롯** 변경 — 오늘 데이터에 근거 없음
- `S01_ENTRY_V3_MODE` 활성화
- `S06_MIN_PRICE_KRW` 변경
- 계좌 대조 검사(`broker_gateway_v1.py:3338`) 완화 — 재시도만 추가
- S02·S03·S05·돈맥·공용매도 파일 열기
- 이미 끝난 6건(맨 위 표) 재작업

## 공통 절차
```
백업 → 편집(바이트 단위, CRLF 보존) → 테스트(기존 실패 개수 대조) → 해시 재봉인 → 63개 재검증
```
- `.cmd` 는 CRLF + ASCII 전용. `sed -i` 금지(CRLF 를 LF 로 바꾼다 — 오늘 실측 사고)
- 기존 테스트 실패는 회귀가 아님: `tests\test_strategy_06_*` 는 9 failed / 17 passed 가 기준선
  (원인은 `Config has no attribute 'tp_pct'` 등 낡은 테스트)

## 우선순위
```
수리 1 (계좌 재시도)  →  수리 2 (ROCKET 기록)  →  수리 3 (표만)
```
