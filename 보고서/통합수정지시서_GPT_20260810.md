# 통합 수정지시서 — 엔진 생존·복구 배관 (2026-08-10 작성)

> **이 문서가 정본입니다.** 앞서 드린 `저장잠금사고_추가수정_GPT전달_20260810.md` 는 이 문서에 흡수했습니다.
> **범위: 엔진이 죽는 문제와 되살리는 배관.** 매매 조건·문턱은 이 문서의 범위가 아닙니다(§6 참조).

---

## 0. 이미 끝난 것 — **다시 손대지 마세요**

`write_json_atomic` / `_write_json_atomic` **10곳 재시도 배선은 완료·검증 통과**입니다.
제가 실제로 파일을 잠그고 시험했습니다(로그 추론 아님, 시험기 `scratchpad\verify_atomic_retry.py`).

| 검증 | 결과 |
|---|---|
| 일시잠금 0.5초 → 저장 성공 | **10/10** (전부 0.6초 안에 성공) |
| 계속잠금 3.0초 → 안 죽고 False 반환 | **10/10** |
| 컴파일 | **10/10** |
| pytest | **9 failed / 554 passed** — 실패 목록이 수정 전과 **완전 동일**(회귀 없음) |
| 호출부 반환값 처리 | **13곳 전부** |

⛔ 이 함수들을 다시 고치지 마세요. 회귀 위험만 있습니다.

---

## 1. 배경 — 오늘 죽은 엔진은 **셋**이었습니다 (전부 증거 확보)

| 엔진 | 사망 | 트레이스백 | 결과 |
|---|---|---|---|
| **S06** 급락저점추격 | 09:20:02 | `LOG\sched_STRATEGY06_LIVE.log` | **실보유 에스피지(058610) 무감시** |
| **S05** 회전엔진 | 10:14 | `data\LOG\sched_STRATEGY05_LIVE.log` (**공용코어**에서) | 보유 0건, 손실 없음 |
| **S04** 신호기 | 12:16 (**2회**) | `data\LOG\sched_STRATEGY04_SIGNAL.log` | S04 자체가 OFF |

셋 다 동일: `os.replace(tmp, path)` → `PermissionError: [WinError 5]`.

원인은 **읽는 쪽**입니다. Windows에서 CPython `open()` 은 `FILE_SHARE_DELETE` 를 주지 않아,
**누가 그 json 을 읽고만 있어도** 이름 바꿔치기가 거부됩니다.

⚠️ **재발 사고입니다.** `strategy_05_base_breakout_signal_v1.py` 주석:
`★[2026-07-29 승인 "재시도 패치"] ... 7/29 11:29 실제 사고·마감까지 낡은 신호로 운행`
**그때 두 곳만 고치고 형제 코드를 놔둬서 12일 뒤 같은 일이 났습니다.**

🔑 이번 지시의 핵심은 "재시도를 더 넣어라"가 아니라
**"죽어도 자동으로 되살아나고, 죽은 사실이 반드시 로그에 남게 하라"** 입니다.

---

## 2. 【지시 A · 최우선】 크래시 복구 그물에 **S05·S06 추가** — 친구님 승인 완료

`RUN\hidden\SAFEPLUS_STRATEGY_ENGINE_RECOVERY.cmd` 는 **09:02~15:12 매분** 실행되며,
주석에 이렇게 돼 있습니다:

> *alive engine = instant exit 0,*
> ***dead engine with positions = takeover in exit-only mode (resume built in)***

즉 **죽은 엔진에 포지션이 있으면 매도전용으로 인수하는 기능이 이미 있습니다.**
그런데 부르는 대상이 넷뿐입니다:

```
start "" /min cmd /c ...\SAFEPLUS_STRATEGY01_LIVE.cmd
start "" /min cmd /c ...\SAFEPLUS_STRATEGY02_LIVE.cmd
start "" /min cmd /c ...\SAFEPLUS_STRATEGY03_LIVE_ASCII.cmd
start "" /min cmd /c ...\SAFEPLUS_STRATEGY04_LIVE.cmd
```

**이 그물이 실제로 작동한다는 증거**: S04 회전엔진은 오늘 죽었는데도
**자동 부활했습니다**(`sched_STRATEGY04_LIVE.log` 14:19 갱신). S05·S06은 목록에 없어 아무도 안 살렸습니다.

### 요청

두 줄 추가:

```
start "" /min cmd /c C:\stock_bot\RUN\hidden\SAFEPLUS_STRATEGY05_LIVE.cmd
start "" /min cmd /c C:\stock_bot\RUN\run_strategy_06_crash_low_chase.cmd
```

### 이중 기동이 안 되는 근거 (제가 미리 확인했습니다)

7/28 사고(브로커 2중 기동)를 반복하면 안 되므로 먼저 확인했습니다:

| 엔진 | ProcessLock | 위치 |
|---|---|---|
| S06 | **있음** | `strategy_06_crash_low_chase_v1.py:303` 정의 · `:2047-2048` 획득 실패 시 종료 |
| S05 회전 | **있음** (2곳) | `strategy_05_rotation_engine_v1.py` |

**둘 다 이미 떠 있으면 스스로 즉시 종료합니다.**

### ⚠️ 그냥 넣지 말고 반드시 확인할 것

1. **실제로 두 번 띄워** ProcessLock 이 막는지 **실측**하세요. 코드만 읽고 판단 금지.
2. **`.cmd` 는 CRLF·ASCII 전용**입니다. 편집 후 CR/LF 개수와 인코딩을 반드시 확인하세요.
3. S05 는 `SAFEPLUS_STRATEGY05_LIVE.cmd` 안에서 `--approve` 를 먼저 부릅니다.
   **매분 재호출될 때 승인이 중복 발급되지 않는지** 확인하세요.
4. **오늘(8/10) 장중에는 배선하지 마세요.** 마감(15:30) 후 또는 내일 아침 기동 전에 반영합니다.

---

## 3. 【지시 B】 S06 전용 보강

S06 은 **공통매도를 안 쓰고 강제청산도 없어**(설계 의도) 엔진이 죽으면
**아무도 그 포지션을 보지 않습니다.** 오늘 에스피지가 정확히 그 상태였습니다.

### B-1. 복구 시 매수 재개 여부를 명확히 할 것 ★판단 필요

`run_strategy_06_crash_low_chase.cmd` 는 **매수·매도를 다 합니다.**
복구 그물이 되살릴 때 매수까지 재개되는 게 맞는지 정리해 주세요.

cmd 주석에 이미 스위치가 있습니다:
```
REM off: create config\strategy_06_off.flag (buys only) / delete approval flag (full shadow)
```
- `strategy_06_off.flag` = **매수만 차단, 매도는 유지** → 매도전용 인수에 그대로 쓸 수 있어 보입니다
- **이 깃발이 실제로 매도를 막지 않는지 코드로 확인**한 뒤 알려 주세요. 배선은 그 다음입니다.

### B-2. 태스크 이름이 규칙에서 벗어나 있습니다

S06 을 띄우는 예약 태스크 이름이 **`STRATEGY06_LIVE`** 로, 다른 것들과 달리 `SAFEPLUS_` 접두어가 없습니다.
**저는 오늘 이것 때문에 S06 태스크를 못 찾아 한참 헤맸습니다.** 점검 스크립트가 `SAFEPLUS_*` 로
훑으면 S06 이 통째로 빠집니다.

→ `SAFEPLUS_STRATEGY06_LIVE` 로 통일할지 검토해 주세요.
⚠️ 이름을 바꾸면 **기존 태스크를 지우고 새로 만드는 것**이므로, 트리거·실행계정·권한이
그대로인지 확인이 필요합니다. **위험 대비 이득을 먼저 평가해서 의견을 주세요.**

### B-3. S06 로그 경로가 혼자 다릅니다

S06만 `C:\stock_bot\LOG\` 로 보내고, 나머지 전략은 전부 `C:\stock_bot\data\LOG\` 입니다(§4 참조).

---

## 4. 【지시 C】 죽은 사실이 반드시 남게

### C-1. 로그가 두 폴더로 갈라져 조사를 놓칩니다

```
C:\stock_bot\LOG\sched_STRATEGY06_LIVE.log        ← S06 만
C:\stock_bot\data\LOG\sched_STRATEGY05_LIVE.log   ← 나머지 전부
C:\stock_bot\data\LOG\sched_STRATEGY04_SIGNAL.log
```

**저는 오늘 `LOG\` 만 뒤지다가 S05·S04 사망을 못 볼 뻔했습니다.**
한쪽으로 통일하거나, 최소한 **양쪽을 같이 훑는 점검 스크립트**를 만들어 주세요.
(통일 쪽이 근본적이지만 소비자가 많다면 점검 스크립트가 안전합니다 — **어느 쪽이 나은지 의견을 주세요.**)

### C-2. `2>&1` 이 빠진 곳

`RUN\hidden\SAFEPLUS_STRATEGY01_BOOTSTRAP.cmd` 는 stderr 리다이렉트가 없어
**죽어도 흔적이 안 남습니다.** 나머지 cmd 는 전부 있습니다.

### C-3. 아침 점검에 "엔진 생존 확인"을 넣어 주세요

지금은 엔진이 죽어도 **몇 시간 뒤에야 사람이 발견합니다**(오늘 4시간 걸렸습니다).
`.tmp` 잔해가 좋은 신호입니다 — **`.tmp` 가 본체보다 1~2초 뒤에 남아 있으면 그 순간 죽은 것**입니다.

---

## 5. 【검토 요청 D】 근본 원인 — 읽는 쪽

쓰는 쪽 재시도는 **증상 완화**입니다. 근본은 **읽는 쪽이 파일을 잡는 것**입니다.
실전 상태 JSON 을 읽는 파일들:

```
RUN\crash_flat_v1.py                       RUN\stockbot_live_broadcast_v1.py
RUN\strategy_03_auto_live_preflight_v1.py  RUN\strategy_04_preflight_v1.py
RUN\strategy_05_preflight_v1.py            (+ 각 전략 엔진 자신)
```

이들이 **복사본을 떠서 읽도록** 바꾸면 잠금 자체가 사라집니다.
다만 파일이 많으니 **바로 배선하지 말고, 범위와 순서를 먼저 제안**해 주세요.

---

## 6. 【판단 대기 — 배선 금지】 매매 조건에 걸리는 것

아래는 **친구님 판단 사항입니다. 절대 먼저 배선하지 마세요.** 의견만 주세요.

### D-1. S06 아침 트레일 평가가 3분 간격입니다

`S06_MORNING_TRAIL_EVAL_SEC = 180`. 오늘 실제로 이 때문에 매도가 밀렸습니다:

- 09:18:02 평가 → 다음 평가 예정 09:21:02
- **09:19:59 에 매도 조건이 이미 성립**(되밀림 1.18% ≥ 문턱 1.0%)
- 09:20:02 엔진 사망 → 영영 평가 안 됨

**그런데 결과는 반대였습니다.** 팔았다면 **+0.4%**, 안 팔려서 지금 **+5.0%** 입니다.
**표본 1건이고 크래시가 만든 우연이므로, 이걸 근거로 간격을 바꾸면 안 됩니다.**
바꾸려면 그림자로 며칠 쌓아 채점하는 게 먼저입니다.

### D-2. S06 은 강제청산이 없습니다

설계 의도(익일 아침까지 보유)입니다. **건드리지 마세요.**
다만 "엔진이 죽으면 그 사이 아무도 안 본다"는 위험은 §2 복구 그물로 해결됩니다.

---

## 7. 하지 말아야 할 것

- ⛔ **`write_json_atomic` 재작업 금지** — 10곳 검증 통과 완료 (§0)
- ⛔ **매매 조건·문턱 변경 금지** — 이번 건은 저장/복구 배관 문제입니다
- ⛔ **오늘 장중(마감 15:30 전) 배선 금지** — 실전 엔진이 돌고 있습니다
- ⛔ **S06 재기동 금지** — 친구님이 보류를 지시하셨습니다. 실보유 에스피지 1건이 걸려 있습니다
- ⛔ **신호기(`_SIGNAL`)를 복구 그물에 그냥 넣지 말 것** —
  `strategy_05_base_breakout_signal_v1.py` 는 **ProcessLock 이 없습니다.** 매분 부르면 다중 기동합니다.
  넣으려면 **잠금을 먼저 넣고** 나서입니다.
- ⛔ 검증 없이 "완료" 보고 금지 — **실제로 돌린 결과**를 붙여 주세요

---

## 8. 완료 증거로 제출할 것

1. 변경 파일 목록과 `git diff`
2. `.cmd` 를 고쳤다면 **CR/LF 개수와 인코딩(ASCII)** 확인 결과
3. **ProcessLock 이중 기동 실측** — 두 번 띄워서 두 번째가 종료되는 화면/로그
4. `pytest` 전체 — **9 failed / 554 passed 유지**(실패 목록이 같아야 합니다)
5. §3·§4 의 "의견을 달라"고 한 항목은 **배선 말고 답변으로** 주세요

---

## 부록 A. 지금 살아 있는 것 / 죽은 것 (8/10 14:2x 기준)

| | 상태 |
|---|---|
| S01·S02·S03·S04 회전엔진 | **살아있음** (복구 그물이 매분 지킴) |
| S02 신호기·SHADOWC, S05 신호기 | **살아있음** |
| **S06** | **09:20 사망 · 에스피지(058610) +5% 무감시** |
| **S05 회전엔진** | **10:14 사망** (보유 0건) |
| **S04 신호기** | **12:16 사망** (S04 자체가 OFF) |

## 부록 B. 이번 범위가 **아닌** 것 (혼선 방지)

아래는 마감 후 별도로 지시할 예정입니다. **이 문서로 착수하지 마세요.**

- 보안 수리 4건 (`broker_gateway_v1.py:2472,2475` NaN 우회 · ACCOUNT_INFO 무인증 · BATCH_TR · RUN 폴더 잠금)
- 종가매수(EOD_GAP) GHOST 7건 및 `MIN_PRICE`
- S01 3단계 수정 실전 확인 (8/11 아침)
- S02 조기매도 대안 채점
- S05 시작 시각 09:25 → 09:00 검토
