# GPT 작업 지시서 — 아침 점검 오탐 제거 (2026-08-18 작성)

작성: Claude / 지시: 친구님
대상: 아침 자동 점검의 **매일 반복되는 거짓 경보** 제거

---

## 1. 결과 (무엇을 만들어야 하는가)

**아침 점검이 "문제 있음"을 띄우면, 그게 진짜 문제여야 한다.**

지금은 매일 아침 거짓 경보 6건이 뜬다. 그래서 사람이 매일 손으로 갈라내야 하고,
진짜 문제가 섞여 들어와도 구분이 안 된다.

목표 상태:
- 정상적인 아침 → `emergency_status_check_v1.py` **결과코드 0**
- 리허설 판정 → 정상 아침에는 **WARN 0건**
- 진짜 고장(자료원 0건, 수집 실패, 해시 변조 등) → **여전히 잡힌다**

---

## 2. 고칠 것 (실측 근거 포함)

### 2-1. 긴급점검판 `RUN\emergency_status_check_v1.py` — 오탐 5건

2026-08-18 08:25 실행 결과 (실측):

```
결론: 문제 6건
  - 차단 플래그 존재: morning_rehearsal_fail.flag   ← 08:30 리허설이 지움. 별개 항목
  - 승인 S01 무효 형식                              ← 오탐
  - 승인 S02 무효 형식                              ← 오탐
  - 승인 S03 무효 형식                              ← 오탐
  - 일봉이 20260814에 멈춤                          ← 오탐
  - 종가매수(eod_gap_track) 최신 20260814           ← 오탐
```

**(A) 승인 플래그 3건 — 시각 때문에 구조적으로 항상 실패**

- 점검판은 **08:25**에 도는데, 플래그 리셋은 **08:45**, 승인 갱신은 **08:59:35**다.
- 따라서 08:25 시점에는 **항상 전날 승인이 남아 있고**, 판정 함수
  `legacy_daily_approval_valid()`(`RUN\approval_settings_guard.py:574-599`)는
  마지막 줄에서 `approved_at.date() == now_kst.date()` 를 요구하므로 **무조건 무효**가 된다.
- 즉 이 시각에 승인 유효성을 묻는 것 자체가 설계 오류다.

**고칠 방향**: 실행 시각에 따라 판정을 나눈다.
- `08:45` 리셋 이전 → **"판정 보류(장 전 정상)"** 로 표기. 문제 건수에 넣지 않는다.
- `08:59:35` 사전점검 이후 → 그때는 오늘 날짜 승인이 있어야 하므로 **지금처럼 엄격 검사**.
- 판정 보류 상태도 화면에는 보여준다(숨기지 말 것). 다만 **결과코드에 반영하지 않는다.**

**(B) 일봉 최신일 / 종가매수 track — "오늘" 기준이라 장 전엔 항상 실패**

- 장 전에 오늘 마감분 일봉이 존재할 수 없다. 매일 아침 100% 실패한다.
- 게다가 휴장 다음날(오늘 8/18, 8/17 대체공휴일)은 이틀 전 날짜가 최신이라 더 크게 어긋난다.

**고칠 방향**: 기준을 **"오늘"이 아니라 "직전 거래일"** 로 바꾼다.
- 오늘 16:05 정규수집 **이후**에만 "오늘 자료"를 기대한다.
- 그 전에는 **직전 거래일 자료가 있으면 정상**이다.
- ⚠️ **휴장일 목록을 새로 만들지 말 것.** 사전점검
  (`RUN\strategy_all_auto_live_preflight_v1.py`)이 이미 휴장일을 판정하고 있다
  (8/17 정상 제외 확인됨). **그 로직·자료를 찾아 재사용**할 것. 중복 정의는 금지.

### 2-2. 리허설 — 일봉 신선도 WARN 이 매일 뜬다

2026-08-18 08:30 실행 결과 (실측, `보고서\아침점검_20260818.txt`):

```
아침 기동 리허설  2026-08-18 08:30:01   판정: WARN
 skip  산출물 신선도       08:32 이전이라 건너뜀
  OK   관리자권한 실행파일  16개 sha256 기준표와 동일
 WARN  자료 신선도(일봉)   최신일 20260814 — 오늘(20260818) 마감분 아직 없음
  OK   자료 신선도(1분봉)  최신일 20260817
  OK   바이트코드 캐시     낡은 .pyc 없음
```

- `자료 신선도(일봉)` 도 **같은 "오늘" 기준 문제**다. 2-1(B)와 동일하게 고친다.
- ✅ 참고: **`관리자권한 실행파일` 검사는 이미 정상 작동**한다(16/16 일치).
  8/17 15:51 재기준선이 잡혀 있다. **이 항목은 건드리지 말 것.**
- ⛔ **`자료 신선도(일봉)` 을 FAIL 로 승격하지 말 것.** 매일 뜨는 항목을 FAIL 로 올리면
  전략이 매일 죽는다. 기준을 고치는 것이 답이지 등급을 올리는 것이 아니다.

### 2-3. 추가할 검사 2개 (사람의 기억을 대체)

지금은 아래 두 가지를 사람이 손으로 확인하고 있다. 점검판에 넣어 자동화한다.

**(C) 관리자권한 실행파일 해시 16개 대조**
- 기준표 `config\elevated_path_hashes.json` 의 `파일` 항목과 실제 SHA256 대조.
- 리허설에는 이미 있으나 **긴급점검판에는 없다**. 점검판에도 넣어 한 곳에서 다 보이게 한다.
- 불일치 1건이라도 → 문제로 계상.

**(D) 수리 반영 여부 자동 확인**
- 배경: 2026-08-18 아침에 Claude 가 "브로커 TR 수리 미착수"라고 잘못 보고했다.
  실제로는 8/14 에 반영돼 있었다(`RUN\backup\broker_gateway_v1_20260814_before_tr_screen_isolation.py`).
- 방침: `RUN\backup\*_before_<수리이름>.py` 백업 파일명을 목록화해
  **"어떤 수리가 언제 들어갔는지"** 를 점검판이 출력한다.
- 이건 **정보 출력**이지 합격/불합격 판정이 아니다. 결과코드에 넣지 말 것.

---

## 3. 제약 (반드시 지킬 것)

1. 🔴 **작업 시각: 15:30 마감 이후에만.** 08:40~15:30 은 실전파일 편집 금지
   (`CLAUDE.local.md` 규칙 1). 오늘 장중에는 읽기 조사만 하라.
2. 🔴 **08:35 워치독 재기동 때 디스크 코드가 그대로 실전에 뜬다.**
   작업이 미완성이면 **반드시 원복**하고 끝내라. 반쯤 고친 상태로 두지 말 것.
3. **대상 파일은 2개뿐**: `RUN\emergency_status_check_v1.py` 와 리허설 스크립트.
   그 외 생산 코드(전략·브로커·주문)는 **한 줄도 건드리지 말 것.**
4. **백업 필수**: `<원본명>_20260818_before_stale_gate.py` 형식으로 남길 것.
5. **휴장일 판정을 새로 구현하지 말 것.** 기존 사전점검 로직 재사용(2-1B 참조).
6. **새 파일·새 추상화·새 패턴 금지.** 기존 코드 스타일 그대로 복사해 쓸 것.
7. 두 파일이 **승인 명부**(`config\live_approved_hashes_v1.json`)에 올라 있는지 먼저 확인하라.
   올라 있으면 수정 후 **반드시 `RUN\approval_manifest_writer_v1.py`(CAS)로만** 갱신.
   직접 편집 금지.
8. 두 파일이 **관리자 기준표**(`config\elevated_path_hashes.json`)에 올라 있는지도 확인하라.
   올라 있으면 수정 후 해시 재등록이 필요하다. **친구님 승인 없이 재기준선을 잡지 말 것.**
9. `.cmd` 는 CRLF · `.ps1`/`.cmd` 는 ASCII 전용 · **PowerShell 로 JSON 쓰기 금지**(BOM 사고 이력).
10. 발견한 다른 문제는 **고치지 말고 한 줄 보고**만 하라.

---

## 4. 왜 이걸 하는가 (이유)

**"매일 뜨는 경보는 무시하게 되고, 결국 검사가 죽는다."**

이 프로젝트는 8/5 리허설에서 이미 이 실패를 겪었다. 지금 아침 점검이 정확히 같은 길에 있다 —
결과코드 1이 **매일** 뜨니까 아무도 안 본다. 그러면 진짜 고장(8/14 의
`SOURCE_EMPTY:high_range_top30` 같은 것)이 와도 똑같이 1로 묻힌다.

경보의 가치는 **"울리면 진짜"** 일 때만 있다.

---

## 5. 완료 증거 (이 형식으로 제출)

말로 "고쳤다"고 하지 말고 **실행 결과**를 붙일 것.

### (1) 변경 diff
두 파일의 수정 전/후 diff 전문.

### (2) 정상 아침 → 조용해야 한다
```
[증거 A] 오늘(2026-08-18) 자료로 08:25 시각을 가정해 실행
  기대: 결과코드 0, 문제 0건
  실제: (붙일 것)
```

### (3) 🔴 진짜 고장 → 여전히 잡혀야 한다 (이게 제일 중요)
오탐만 없애고 진짜 경보까지 죽이면 **작업 실패**다. 아래를 반드시 증명하라.

```
[증거 B] 8/14 사고 상황 재현 — 고저폭판 후보 0건
  기대: 문제로 계상, 결과코드 1
  실제: (붙일 것)

[증거 C] 일봉이 직전 거래일에도 없는 경우(진짜 수집 실패)
  기대: 문제로 계상, 결과코드 1
  실제: (붙일 것)

[증거 D] 관리자 해시 1건 불일치 상황
  기대: 문제로 계상, 결과코드 1
  실제: (붙일 것)

[증거 E] 09:10 시각 가정 + 승인 플래그가 전날 것
  기대: 문제로 계상 (이 시각엔 오늘 승인이 있어야 정상)
  실제: (붙일 것)
```

### (4) 휴장 다음날 시나리오
```
[증거 F] 8/18 처럼 직전 거래일이 이틀 전인 경우
  기대: 정상 판정 (결과코드 0)
  실제: (붙일 것)
```

### (5) 회귀 확인
```
- pytest 실행 결과 (기존 실패 건수와 대조할 것 — 기존 실패에 새 실패가 숨는다)
- 승인 명부 / 관리자 기준표 등록 여부와 처리 내역
- 백업 파일 경로
```

---

## 6. 하지 말 것 (기각 이력)

- ⛔ `자료 신선도(일봉)` 을 FAIL 로 승격 — 매일 전략이 죽는다
- ⛔ 휴장일 목록 새로 정의 — 기존 것과 어긋나면 사전점검과 판정이 갈린다
- ⛔ 오탐을 "검사 삭제"로 해결 — 기준을 고치는 것이지 검사를 없애는 게 아니다
- ⛔ 전략·브로커·주문 코드 수정
- ⛔ 장중(08:40~15:30) 편집

---

## 부록: 오늘 아침 실측 기록 (참고용)

2026-08-18 정상 아침의 실제 값. 판정 기준을 잡을 때 참고하라.

```
08:25:25  긴급점검판 실행         결과코드 1 (문제 6건, 전부 위에 열거)
08:30:30  리허설                 결과코드 0, 판정 WARN (일봉 신선도 1건)
08:35:35  브로커 워치독            267009 = 실행 중 (정상)
08:40:40  고저폭판/파이프라인       267009 = 실행 중
08:42     돈맥_1분봉.json 실시간 갱신 확인 (브로커 TR 정상 작동 증거)
08:45     strategy_0{1,2,3}_off.flag 생성 (닫힘이 기본값 — 설계대로)
08:55     strategy_06_live_approved.flag 갱신 (오늘 날짜)
08:59:35  사전점검 — 이 지시서 작성 시점에는 아직 미실행

일봉 품질: 20260814 행 1632건, 빈껍데기 36건(2.2%) — 평시 수준
고저폭판: 42건, source 20260814 (직전 거래일)
관리자 해시: 16/16 일치
```
