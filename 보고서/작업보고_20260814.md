# 2026-08-14 (금) 작업 보고서

작성: Claude / 08:00 ~ 15:40

---

## 0. 한 줄 요약

**고장 6건을 찾아 고쳤고, 그중 3건은 3일~3주째 방치돼 있던 것이다.**
종가매수는 원인을 찾아 수리했고 **월요일 15:18이 3주 만의 첫 시험**이다.
브로커 TR 막힘 하나가 미해결로 남았다.

---

## 1. 오늘 실제 매매

| 종목 | 진입 | 청산 | 결과 |
|---|---|---|---|
| 네오오토(212560) | 10:05 11,370 | 10:15 11,090 | **-2.46%** 하드스톱 |
| 하나마이크론(067310) | 10:08 35,200 | 10:56 36,000 | **+2.27%** 꼭지매도 |
| 지엔씨에너지(119850) | 11:15 53,600 | 11:38 52,600 | **-1.87%** 하드스톱 |
| LS마린솔루션(060370) | 11:12 32,450 | 보유 | — |
| 태성(323280) | 10:08 50,300 | 보유 | — |
| 싸이맥스(160980) | 11:40 33,150 | 보유 | — |

- 전부 S02. S01은 관문 사고로 진입창(09:00~09:20)을 통째로 날렸다.
- S03 신호 5건 발생했으나 미체결. S06은 대상 0(고저폭판 0건).
- **매도 엔진은 규칙대로 정확히 작동했다.** 손절 -2%, 꼭지매도 전부 설계대로.

---

## 2. 고친 것 (7건)

### ① 일봉 3일 결손 → 복구 (08:33)
- 8/12·8/13 누락. 08:33 수집 성공 (QA 100, 1,630종목, 403,767행).
- 원인은 브로커 TR 막힘 + 8/13 21:49 PC 절전.

### ② 리허설 계약 위반 → 전략 3개 OFF 상태 해제 (09:32)
- **원인**: 8/13 S01 MA5 매도보류 배선이 상승보유 조건에 `or`를 붙여
  리허설 계약("조건식은 daily_ma_permit 단독")을 위반.
- 08:30 리허설 FAIL → 08:45 S01·S02·S03 자동 OFF → 09:00 SHADOW 모드.
- **수리**: `or` 가지를 독립 if 두 개로 분리. **동작은 동일**, 계약만 복원.
- 명부 CAS 갱신 + 관문 4종 PASS + 엔진 재기동 + 승인 플래그 재발급.
- 백업 `strategy_common_hold_sell_v1_20260814_before_boolop_split.py`

### ③ S02 신호 생성기 정지 → 재기동 (09:53)
- `PermissionError [WinError 5]` 저장잠금으로 신호 파일이 어제 14:21에서 멈춰 있었다.

### ④ S02 잔고조회 실패 → 재시도 강화
- 8/14 실측 28건 실패. 09:55~09:59에만 6건 몰려 **매수 6건 무산**(저점 구간).
- **수리**: prebuy 조회 `2.0초x2회` → `1.3초x3회` (실패확률 p²→p³, TTL 4.7초<5초 유지).
- 백업 `strategy_common_order_v1_20260814_before_prebuy_retry3.py` / 명부 갱신 완료.

### ⑤ 1분봉 3일 결손 → 복구 (14:42)
- **원인 두 겹**:
  - 8/11 TR-ROLLBACK 이 절반만 됨 — 보호목록에서 뺐는데 클라이언트는 계속 서명 시도
    → `this request type does not use IPC authentication` 예외 → 8/12부터 전량 차단(886건).
  - **구 수집기 프로세스가 안 죽고 옛 코드로 계속 돌고 있었다** (GPT가 잡음).
- **수리**: 서명 범위 검사 추가 (3파일) + 구 프로세스 종료.
- 백업 3건 (`..._20260814_before_sign_scope.py`)

### ⑥ 낡은 바이트코드 캐시 → 10개 격리
- `__pycache__`에 8/10자 `.pyc`가 남아 **새 코드를 가리고 있었다.**
- 오늘 같은 수정을 세 번 "적용됐다"고 오판한 원인.
- GPT가 새벽에 고친 일봉 수리 2건과 브로커 본체도 가려져 있었다.
- `backup\pyc_20260814\` 로 격리 (되돌리기 가능).

### ⑦ 종가매수 3주 무매수 → 원인 확정 + 수리
- **원인**: 7/29에 LOCKED-FIRST(상한가 우선) 경로를 만들었으나
  점수검사 `_passes_final_score`(MIN_SCORE 70)를 그대로 공유 →
  **한 번도 발동 못 함.** 상한가는 거래가 잠겨 거래대금 점수를 못 받아 최대 28점대.
  (8/14 실측 5개: 28.2 / 22.5 / 21.1 / 19.8 / 18.9 → 전부 탈락, NO_TRADE)
- **백테로 검증** (신설 `RUN\eodgap_limitup_score_backtest_v1.py`, 일봉 1년):
  ```
  상한가 진입 전수 1,527건 · D종가매수→D+1시가매도 · 비용 0.38%p 차감
    전수 평균 +7.543% / 중앙값 +5.978% / 승률 79.1%
    거래대금 5분위(=실전 점수의 주재료):
      Q1(최저 ~3,103억)  +12.492%  승률 84.3%   ← 문턱에 걸려 못 사던 쪽
      Q5(최고 69,327억~)  +3.669%  승률 69.0%   ← 점수 높아 통과하던 쪽
    단조 역상관. 점수 낮은 상한가가 3.4배 좋다. 13개월 전부 플러스.
  ```
- **수리**: `LOCKED_MIN_SCORE`(기본 0=면제) 신설. LOCKED 경로만 적용.
  `MIN_SCORE`(일반 경로)는 손대지 않았다.
- 롤백 `setx EOD_GAP_LOCKED_MIN_SCORE 75`

---

## 3. 새로 만든 것 (6건, 전부 읽기전용·주문 0)

| 파일 | 용도 | 배선 |
|---|---|---|
| `emergency_status_check_v1.py` | 관문·플래그·일봉·고저폭판을 한 번에 점검 | 매일 08:25 |
| `emergency_reopen_s0123_v1.ps1` | 전략 자동 OFF 시 4겹 잠금 해제 (기본 예행) | 수동 |
| `DOCS\EMERGENCY_REOPEN_RUNBOOK_20260814.md` | 위 절차서 (승인 플래그 정확한 형식 포함) | — |
| `s01_shadow_outcome_tracker_v1.py` | S01 무눌림 그림자 성과 계산 | 매일 15:40 |
| `s02_candle_block_shadow_v1.py` | S02 봉전환 매수차단(B안) 그림자 | 매일 09:00~14:25 |
| `eodgap_limitup_score_backtest_v1.py` | 상한가 점수 문턱 백테 | 수동 |

**리허설 검사 3종 추가** (전부 WARN — 전략을 멈추지 않음):
일봉 신선도 / 1분봉 신선도 / **낡은 pyc 탐지**

---

## 4. 발견했으나 아직 안 고친 것

### 🔴 브로커 TR 대기루프 공유 (최대 미해결)
- 오늘 실측: `tr_loop timeout` 166건, `BROKER-DEAD` 92회, `CIRCUIT-BREAK` 86회.
- 8/5 코드 주석에 원인이 이미 규명돼 있다 — 모든 TR이 대기 루프 하나를 공유해
  **남의 응답이 내 대기를 깨우고 가짜 TIMEOUT이 된다.** 근본 수리만 9일째 미착수.
- ⚠️**브로커 로그로는 이 고장이 안 보인다.** 브로커는 "응답 보냄"으로 기록하고
  클라이언트만 TIMEOUT을 본다. 오늘 GPT가 브로커 로그만 보고 "정상"이라 오판했다.
  **판단은 결과물로** — `prices_1m.csv`가 쌓이는가, 사이클당 정상봉 수.
- 지시서: `보고서\브로커수리_지시서_GPT_20260814.md`

### 🟡 고저폭판이 당일 미완성 일봉을 물음
- 아침 수집이 장 전에 끝나면 당일 행이 OHLC 전부 전일종가·거래량0으로 들어간다.
  → 고저폭 0% → 후보 0건 → **S06 대상 0**. 오늘이 그랬다.
- `source_stale`(과거)만 검사하고 "미래(당일)"는 안 막는 것이 결함.

### 🟡 종가매수 그림자에 성과 기록이 없음
- `eod_gap_leader_shadow.csv`는 후보·점수만 적고 **진입가·수익률 컬럼이 없다.**
  "무엇을 살 뻔했나"만 있고 "얼마 벌었나"가 없어 판단 근거가 안 된다.
- 오늘은 백테로 대신했다. 그림자 자체 보완은 별도 안건.

### 🟡 S01 무눌림 차단이 후보의 34%를 잘라냄
- 오늘 탈락 사유 1위 `PULLBACK_BELOW_OPEN_NOT_SEEN` 73건 (전체 215건 중).
- 그림자 34건 기록 중. 성과 추적기가 15:40부터 자동 계산.

---

## 5. 오늘 배운 것 (반복 금지)

1. 🔑 **"파일 고쳤다" ≠ "적용됐다"** — 낡은 `.pyc`가 새 코드를 가린다.
   같은 수정을 세 번 "적용됐다"고 오판했다. 이제 리허설이 잡아준다.
2. 🔑 **"만들었다" ≠ "돈다"** — 도구 3개를 만들고 태스크 등록을 빼먹었다.
   도구를 만들면 **배선까지가 한 벌**이다.
3. 🔑 **성과 판단은 실전 프레임으로** — 실보유 중앙값 20분인데 종가로 재면 안 된다.
4. 🔑 **"절반만 수리"가 오늘만 두 번** — 8/11 TR 롤백, 7/29 상한가 우선 경로.
   둘 다 한쪽만 바꾸고 짝을 안 맞춰 기능이 죽어 있었다.
5. 🔑 **로그가 아니라 결과물로 판단하라** — 브로커 로그는 정상인데 봉은 0개였다.

---

## 6. 월요일(8/17) 확인 목록

메모리 `stockbot-20260817-monday-morning-checklist` 에 상세 기록.
Claude가 **먼저 꺼내서** 확인한다.

**아침**: 긴급점검판(08:25) / 리허설 신규검사 3종 오탐 여부 / `config\*.flag` /
09시대부터 1분봉 쌓이는가 / S02 잔고실패 감소 / S01 진입창 정상

**15:18 ← 종가매수 핵심**: `[LOCKED-FIRST] 상한가 N개 중 상위 1 매수 (문턱 0)` 발동 확인,
**주문이 나가고 체결되는가**. ⚠️일봉 백테는 "그 가격에 살 수 있는가"를 증명 못 한다.
상한가는 호가가 잠겨 체결 자체가 안 될 수 있다. **체결이 진짜 검증이다.**

**마감 후**: S01 그림자 성과(10·30분 주력) / 봉전환 그림자 표본 / 매매 재생 검증
