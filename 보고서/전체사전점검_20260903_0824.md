# 전체 사전점검 — 2026-09-03 장전

판정: FAIL. 코드·실전 설정·승인/차단 플래그 변경 없음. 전략 엔진 실행·실제 주문·브로커 TR 없음.

## 검출된 실패 2건

1. S07 진입점 `RUN/strategy_07_morning_trend_v1.py`에 검사기가 요구하는 sys.path 부트스트랩이 없다. `live_owner_approval_guard_v1`, `s07_morning_live_orders_v1` 등 로컬 모듈 참조가 있다. 예약 실행문은 `C:/python310/python.exe`로 파일을 직접 실행한다. 해당 Python의 `python310._pth`에는 python310.zip, 현재 런타임 디렉터리, import site만 있다. 실제 주문 가능한 엔진 실행은 하지 않았으며, 오류 분류는 기존 정적 검사 결과다.
2. `RUN/hidden/SAFEPLUS_STRATEGY03_LIVE_ASCII.cmd`의 해시가 `config/elevated_path_hashes.json` 기준표와 다르다. 별도의 실전 승인 해시 검사는 S03을 포함해 통과했다. 기준표 불일치가 관측된 것이며 임의로 기준표를 갱신하지 않았다.

08:30 정규 리허설은 FAIL 결과가 있으면 `config/morning_rehearsal_fail.flag`를 쓰도록 구현돼 있다. 이번 수동 점검은 검사 함수만 호출했고 그 플래그는 생성하지 않았다.

## 통과한 범위

- RUN의 Python 소스 316개 문법 검사, import 대상 존재, order_prefix 등록.
- 저점찾기 계약, 공통배관 계약, 매도 계약.
- S01·S02·S03·S06 승인 해시 및 별도 S07M 승인 해시.
- 최근 거래일 20260902의 S06 승인·상태·실행·복구 산출물.
- 필수 예약작업 26개 전부 존재·활성: S01~S07 실행/신호 관련 작업, 공통 사전점검, 권한별 자체점검, 리허설, 브로커 감시, 파이프라인, 복구, 자료 수집.
- 최근 20분 수정 로그는 0개였다. 로그 검사 PASS는 실제 엔진 기동 성공의 증거가 아니다.

## 경고와 확인 한계

- 일봉·1분봉의 파일 꼬리에서 확인한 최신일은 20260902. 일봉 검사는 오늘 마감 자료가 없다는 경고를 냈으나 점검 시점은 장전이다.
- 소스보다 오래된 바이트코드 캐시 12개가 검출됐다. 캐시 경고만으로 실제 옛 코드 실행을 확정하지 않았다.
- 예약작업 이력에 `SAFEPLUS_FLOW_TREND_INTRADAY_BOARD`, `SAFEPLUS_STRATEGY_FLOW_PRIORITY_SHADOW`의 최근 반환코드 1이 남아 있다. 원인 조사·수정은 하지 않았다.
- `SAFEPLUS_BROKER_NIGHT_STOP` 최근 반환코드는 2147946720이다. 이번 점검에서는 원인을 판정하지 않았다.
- S07 실행·스코어보드 예약작업에는 실행 이력이 없으며 반환코드는 267011이다.
- 08:22:23 예약작업 스냅샷에서 실행 중인 대상 예약작업과 stock_bot Python 프로세스는 없었다. 브로커 연결·잔고·미체결·오늘의 호가/신호 신선도는 미확인이다.
- 산출물 신선도 검사는 08:32 이전이어서 SKIP. 현재 상태를 장중 준비 완료로 판단하지 않는다.

## 실행과 근거

기존 검사기: `C:/stock_bot/RUN/morning_preflight_rehearsal_v1.py`의 검사 함수 전체를 호출. main은 승인/차단 플래그를 변경하므로 호출하지 않았다. Python 감사 훅으로 파일 변경과 네트워크 연결을 차단했다.

최초 필수 태스크 조회는 접근 권한으로 실패했다. 이후 권한을 확보한 읽기 전용 Get-ScheduledTask/Get-ScheduledTaskInfo 결과로 필수 26개를 확인했다. 최초 보고서 쓰기도 권한으로 실패했으며 검사 자체를 다시 실행하지 않고 관측 결과를 이 보고서에 기록했다.

예약작업 원본 스냅샷: `C:/stock_bot/보고서/전체사전점검_예약작업_20260903_082223.json`

S07 해시: `live_owner_approval_guard_v1.verify_live_hashes('S07M')` → true, errors=[]

기존 리허설 16개 결과 항목은 예약작업 조회 경고를 후속 확인으로 해소하면 PASS 11 / FAIL 2 / WARN 2 / SKIP 1이다. 추가 S07 해시 검사 및 예약작업 확대 확인은 이 16개 집계와 구별한다.
