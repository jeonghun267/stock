# GPT 작업 지시문 (3차·확정) — 2026-08-20 밤. 2차 지시문 폐기, 이 문서가 유일본.

> 전부 실측 검증된 문제만이다. GPT의 반박 3건 중 ①(EARLY 모순)·③(지수 14:35 중단)은 수용해 반영했고,
> ②(자료 8/18~20뿐)는 사실이 아니다 — `LOG\strategy_06_crash_low_chase.log`가 8/1부터 보존돼 있다(실측).
> 여기 없는 것은 수정 금지. 발견한 다른 문제는 한 줄 보고만.

---

## A. S06 기동사(ValueError) 방지 — 런처 일괄 수정 (최우선)

`RUN\run_strategy_06_crash_low_chase.cmd` 를 아래 5줄로 정합시킬 것.
(4줄만 바꾸면 EARLY 1.8 ≥ CHASE 1.5 로 `__post_init__` 검증에서 기동 즉사 — GPT 지적이 맞다.)

```
set S06_REBOUND_PCT=0.5
set S06_ENTRY_FLOOR_PCT=0.5
set S06_CHASE_CAP_PCT=1.5
set S06_EARLY_ENTRY_CAP_PCT=1.3   ← 기본안: 옛 구조의 "포기선 -0.2" 간격 보존.
                                     GPT 판단으로 1.1(옛 비율 60% 지점)~1.3 사이 선택 가능하되
                                     선택 근거 1줄을 보고에 남길 것. 0(끄기)은 친구님 승인 없이는 금지.
set S06_TP_PCT=5.0
```

- `RUN\hidden\SAFEPLUS_STRATEGY06_RECOVERY.cmd` 에 같은 set 줄이 있으면 동일하게.
- 수정 후 `__post_init__` 검증 통과를 실제 import 로 확인 (32/64비트 중 S06 실행 파이썬으로).
- .cmd = CRLF·ASCII. 확인값 보고.
- 백업: `RUN\backup\` 에 수정 전후 스냅샷 + 원복 한 줄.

**완료 증거**: 런처 diff + 설정 검증 import 출력(에러 0) + CRLF/ASCII 값.

## B. 승인 명부 CAS 일괄 갱신 — 모든 파일 작업이 끝난 "마지막"에 1회

현재 명부 불일치(실측): **S06** 엔진 · **S01** 4건(open_surge_signal_v2 · signal_contract_v2 ·
SIGNAL 런처 · LIVE 런처) (+ `low_rebound_common_v1.py` 가 등재돼 있으면 포함).
S02·S03은 이미 갱신돼 있다 — 건드리지 말 것.

- 반드시 `RUN\approval_manifest_writer_v1.py`(CAS)로만.
- A·C 작업 완료 후의 최종 해시로 갱신 (순서 엄수 — 갱신 후 파일을 또 만지면 다시 불일치).

**완료 증거**: 갱신 후 전 항목 해시==파일 대조 출력 (S01·S06 전부 OK).

## C. S01 강한놈우선(TREND_PRIORITY) — 친구님 지시와 SHADOW 강등의 충돌 해소

친구님이 오늘 장중 명시 지시로 LIVE 배선을 명령했고 반영됐었다. 이후 GPT가
"재생 통과 전 SHADOW"로 되돌렸다(현재 런처 8행). 규율은 이해하나 친구님 지시가 우선이다. 절충:

1. 오늘 밤 저장입력 재생을 돌릴 것 — `RUN\s01_open_priority_prod_replay_v1.py`
   (order_signals(mode="LIVE") 경로, 캡처는 `data\audit\s01_open_priority\20260819·20260820.jsonl` 존재).
2. **PASS → 런처를 `S01_TREND_PRIORITY_MODE=LIVE` 로 복원** (친구님 지시 이행).
3. FAIL → SHADOW 유지 + 실패 원문을 아침 보고 최상단에 올릴 것 (친구님이 직접 판단).

**완료 증거**: 재생 결과 원문 + 런처 최종 상태.

## D. B-1 재수행 — 자료는 있다

`LOG\strategy_06_crash_low_chase.log` (8/1~현재 보존)에서 최근 5거래일(8/13·14·18·19·20)
CHASE_TRIGGER 종목의 "첫 반등 폭" 분포를 산출해 보고할 것.
목적: 새 문턱(반등 0.5%)에서 ③눌림저점 +0.3% 조건이 만드는 실질 문턱(~0.7%)이
의도와 맞는지 친구님이 판단할 근거. 수정 금지, 분포 숫자만.

## E. 코스닥 지수 "성공 가장 실패" 원인 규명 (조사·보고, 수리는 원인 확정 후)

실측: `kosdaq_index.json` 마지막 갱신 14:35:02. 그런데 수집 태스크(SAFEPLUS_KOSDAQ_IDX_REFRESH)는
16:00까지 rc=0 "성공" 보고 — 14:40 이후 실행들이 조용히 빈손이었다 (8/5 wmic 빈손 사고와 같은 종).
→ 이것이 14:47부터 regime band=NO_INDEX 의 직접 원인 (S02 적응바닥의 UNKNOWN 차단으로 이어짐).

`fetch_kosdaq_index.py` 가 실패를 삼키는 지점을 찾아 보고할 것. 아침에 재발하면 S02가 차단되므로
내일 08:59 전에 원인이 나와야 한다. 수리안은 보고만 — 배선은 친구님 승인 후.

## F. S02 낡은 주석 1줄 정리 (경미)

`strategy_02_low_buy_signal_v1.py` 295행 부근 "[UNVERIFIED] 4개 중 1개만 재현" 주석을
재생 PASS 사실(prod_replay_s02_exact_inputs_20260820.json)로 갱신. 로직 무변경.

---

## 공통 제약

1. S06 엔진 파이썬 판정 로직 수정 금지(친구님 영역). S02 적응바닥 수정 금지(재생 PASS 확인됨).
2. 명부는 CAS만 · .cmd CRLF/ASCII · PowerShell 로 JSON 금지 · 트레일/매도 규칙 접근 금지.
3. pytest 기준선 38 failed — 신규 실패 0.
4. 완료 보고: 항목별 "diff 요약 · 실행 증거 · 롤백 한 줄".
