# 저장 잠금 사고 — 추가 수정 요청 (2026-08-10 14:2x)

## 0. 먼저: 이미 끝난 것 — **다시 손대지 마세요**

`write_json_atomic` / `_write_json_atomic` **10곳 재시도 배선은 완료·검증 통과**입니다.
제가 실제로 파일을 잠그고 시험했습니다(로그 추론 아님).

| 검증 항목 | 결과 |
|---|---|
| 일시잠금 0.5초 → 저장 성공 | **10/10** (전부 0.6초 안에 성공, 저장됨=True) |
| 계속잠금 3.0초 → 안 죽고 False 반환 | **10/10** |
| 컴파일 | **10/10** |
| pytest | **9 failed / 554 passed** — 실패 목록이 수정 전과 **완전 동일**(회귀 없음) |
| 호출부 반환값 처리 | **13곳 전부** 받음 (조용한 실패 없음) |

시험기: `scratchpad\verify_atomic_retry.py`

---

## 1. 오늘 실제로 죽은 엔진은 **셋**이었습니다 (전부 같은 원인, 증거 확보)

| 엔진 | 사망 시각 | 트레이스백 위치 | 실패한 파일 |
|---|---|---|---|
| **S06** 급락저점추격 | 09:20:02 | `LOG\sched_STRATEGY06_LIVE.log` | `strategy_06_..._state_v1.json` |
| **S05** 회전엔진 | 10:14 | `data\LOG\sched_STRATEGY05_LIVE.log` | `strategy_05_rotation_state_v1.json` |
| **S04** 신호기 | 12:16 (**2회**) | `data\LOG\sched_STRATEGY04_SIGNAL.log` | `strategy_04_pullback_signal_v1.json` |

셋 다 동일: `os.replace(tmp, path)` → `PermissionError: [WinError 5]`.
S05는 **공용코어**(`strategy_01_rotation_engine_v2.py:112`)에서 터졌습니다.

> **원인은 십중팔구 "읽는 쪽"입니다.** Windows에서 CPython `open()` 은
> `FILE_SHARE_DELETE` 를 주지 않아, **누가 그 json 을 읽고만 있어도**
> `MoveFileExW(REPLACE_EXISTING)` 가 ACCESS_DENIED 로 거부됩니다.
> 오늘 저(클로드)의 진단 조회와 중계방송(3분 간격)이 그 시간대에 계속 읽고 있었습니다.

⚠️ **이건 재발 사고입니다.** `strategy_05_base_breakout_signal_v1.py` 와 `골짜기_급반등.py`
주석에 `★[2026-07-29 승인 "재시도 패치"] ... 7/29 11:29 실제 사고·마감까지 낡은 신호로 운행`
이 남아 있습니다. **그때 두 곳만 고치고 형제 코드를 안 고쳐서 12일 뒤 같은 일이 났습니다.**

---

## 2. 【1순위】 크래시 복구 그물에 **S05·S06 이 빠져 있습니다**

`RUN\hidden\SAFEPLUS_STRATEGY_ENGINE_RECOVERY.cmd` 는 **09:02~15:12 매분** 실행되며
주석에 이렇게 돼 있습니다:

> *Idempotent by design: ... alive engine = instant exit 0,*
> ***dead engine with positions = takeover in exit-only mode (resume built in)***

즉 **죽은 엔진에 포지션이 있으면 매도전용으로 인수하는 기능이 이미 있습니다.**
그런데 부르는 대상이 넷뿐입니다:

```
start "" /min cmd /c ...\SAFEPLUS_STRATEGY01_LIVE.cmd
start "" /min cmd /c ...\SAFEPLUS_STRATEGY02_LIVE.cmd
start "" /min cmd /c ...\SAFEPLUS_STRATEGY03_LIVE_ASCII.cmd
start "" /min cmd /c ...\SAFEPLUS_STRATEGY04_LIVE.cmd
```

**S05·S06 이 없습니다. 그래서 오늘 아무도 안 살렸습니다.**
반대로 S04 회전엔진은 목록에 있어서 **지금도 살아 있습니다**(`sched_STRATEGY04_LIVE.log` 14:19 갱신).
이 그물이 실제로 작동한다는 증거입니다.

### 요청

`SAFEPLUS_STRATEGY_ENGINE_RECOVERY.cmd` 에 두 줄 추가:

```
start "" /min cmd /c C:\stock_bot\RUN\hidden\SAFEPLUS_STRATEGY05_LIVE.cmd
start "" /min cmd /c C:\stock_bot\RUN\run_strategy_06_crash_low_chase.cmd
```

### 이중 기동이 안 되는 근거 (제가 확인했습니다)

7/28 사고(브로커 2중 기동)를 반복하면 안 되므로 먼저 확인했습니다:

| 엔진 | ProcessLock | 위치 |
|---|---|---|
| S06 | **있음** | `strategy_06_crash_low_chase_v1.py:303` 정의, `:2047-2048` 획득 실패 시 종료 |
| S05 회전 | **있음** (2곳) | `strategy_05_rotation_engine_v1.py` |

**둘 다 이미 떠 있으면 스스로 즉시 종료합니다.** 그래서 매분 호출해도 안전합니다.

### ⚠️ 반드시 확인해야 할 것 (그냥 넣지 마세요)

1. **S06 은 매수도 재개됩니다.** `run_strategy_06_crash_low_chase.cmd` 는 매수·매도를 다 합니다.
   복구로 되살아났을 때 **매도전용으로만 인수**하려면 별도 스위치가 필요한지 확인하세요.
   (cmd 주석: `off: create config\strategy_06_off.flag (buys only)`)
2. **`.cmd` 는 CRLF·ASCII 전용**입니다. 편집 후 줄끝과 인코딩을 반드시 확인하세요.
3. 추가 후 **실제로 두 번 띄워** ProcessLock 이 막는지 실측하세요. 코드만 읽고 판단 금지.
4. S05 는 `SAFEPLUS_STRATEGY05_LIVE.cmd` 안에서 `--approve` 를 먼저 부릅니다.
   매분 재호출될 때 **승인이 중복 발급되지 않는지** 확인하세요.

---

## 3. 【2순위】 신호기(SIGNAL)는 복구 그물이 아예 없습니다

복구 대상은 `_LIVE`(회전엔진)뿐이고, `_SIGNAL`(신호기)은 하나도 없습니다.
그래서 **S04 신호기는 12:16에 죽은 채 지금까지 그대로**입니다.

신호기가 죽으면 회전엔진은 **낡은 신호 파일을 계속 읽습니다** — 7/29 사고 기록의
"마감까지 낡은 신호로 운행"이 정확히 이 상태입니다. 조용해서 더 위험합니다.

### 요청
신호기도 복구 대상에 넣을지 검토해 주세요. 넣는다면 **신호기마다 ProcessLock 이 있는지 먼저 확인**해야 합니다
— `strategy_05_base_breakout_signal_v1.py` 는 **ProcessLock 이 없습니다.** 그대로 매분 부르면 다중 기동합니다.

---

## 4. 【3순위】 로그가 두 폴더로 갈라져 조사를 놓칩니다

```
C:\stock_bot\LOG\sched_STRATEGY06_LIVE.log        ← S06
C:\stock_bot\data\LOG\sched_STRATEGY05_LIVE.log   ← S05
C:\stock_bot\data\LOG\sched_STRATEGY04_SIGNAL.log ← S04
```

**저는 오늘 `LOG\` 만 뒤지다가 S05·S04 사망을 못 볼 뻔했습니다.**
사고 조사 때 절반을 놓치는 구조입니다. 한쪽으로 통일하거나, 최소한
**양쪽을 같이 훑는 점검 스크립트**가 필요합니다.

또 `run_strategy_06_crash_low_chase.cmd` 만 `LOG\` 로 보내고 나머지는 `data\LOG\` 입니다.

---

## 5. 【4순위】 근본 원인 — 읽는 쪽

쓰는 쪽에 재시도를 넣은 건 **증상 완화**입니다. 근본은 **읽는 쪽이 파일을 잡는 것**입니다.

실전 상태 JSON 을 읽는 파일들(확인된 것):
```
RUN\crash_flat_v1.py                    RUN\stockbot_live_broadcast_v1.py
RUN\strategy_03_auto_live_preflight_v1.py   RUN\strategy_04_preflight_v1.py
RUN\strategy_05_preflight_v1.py         (+ 각 전략 엔진 자신)
```

검토 요청: 이 소비자들이 **복사본을 떠서 읽도록** 바꾸면 잠금 자체가 사라집니다.
비용은 복사 한 번뿐이고, 부작용이 없습니다. 다만 파일이 많으니 **범위와 순서를 먼저 제안**해 주세요.

---

## 6. 【5순위】 자잘한 것

- `.tmp` 잔해 3개: `data\strategy_0{4,5,6}_*.json.tmp`. 다음 저장 때 덮어써지므로 무해하지만,
  **"tmp 가 본체보다 1~2초 뒤에 남아 있다" = 그 순간 죽었다는 신호**라서 진단에 쓸 수 있습니다.
  지울지 남길지 판단해 주세요.
- 재시도 소진 시 알림이 stderr 뿐입니다(`ATOMIC_WRITE_RETRY_EXHAUSTED`).
  8/11 아침 `sched_*.log` 에 이게 찍히면 **1초 재시도로도 부족하다는 뜻**이니 대기 시간을 늘려야 합니다.
- `SAFEPLUS_STRATEGY01_BOOTSTRAP.cmd` 는 `2>&1` 이 없어 **죽어도 흔적이 안 남습니다.**

---

## 7. 하지 말아야 할 것

- ⛔ **매매 조건·문턱 변경 금지** (이번 건은 저장/복구 배관 문제입니다)
- ⛔ **지금 도는 엔진 건드리기 금지** — 장중입니다(마감 15:30)
- ⛔ **S06 재기동 금지** — 친구님이 보류를 지시하셨습니다. 실보유 에스피지(058610) 1건이 걸려 있습니다
- ⛔ **`write_json_atomic` 재작업 금지** — 이미 10곳 검증 통과했습니다
- ⛔ 검증 없이 "완료" 보고 금지 — **실제로 돌린 결과**를 붙여 주세요

## 8. 완료 증거로 제출할 것

1. 변경 파일 목록과 `git diff`
2. `.cmd` 를 고쳤다면 **CR/LF 개수와 인코딩** 확인 결과
3. ProcessLock 이중 기동 **실측 결과**(두 번 띄워서 두 번째가 종료되는지)
4. `pytest` 전체 — **9 failed / 554 passed 유지**(실패 목록이 같아야 합니다)

---

## 참고: 지금 살아 있는 것 / 죽은 것 (14:2x 기준)

| | 상태 |
|---|---|
| S01·S02·S03·S04 회전엔진 | **살아있음** (복구 그물이 매분 지킴) |
| S02 신호기·SHADOWC, S05 신호기 | **살아있음** |
| **S06** | **09:20 사망 · 에스피지(058610) +5% 무감시** |
| **S05 회전엔진** | **10:14 사망** (보유 0건이라 손실 없음) |
| **S04 신호기** | **12:16 사망** (S04 자체가 OFF) |
