# S01(시가급등) 약점 전수 점검 — 2026-08-27 저녁

- 요청: 친구님 "전략 1번 문제점이나 약점을 찾아줘"
- 범위: S01 코드 6파일(신호기 v2 61KB·회전엔진 v2 124KB·v3 진입 4파일) + 그림자 실측 5일치(8/21~8/27). **코드 수정 0줄 (읽기 전용 점검).**
- 방법: 대형 2파일은 검토 에이전트 2개 병렬 정독(재현 스크립트 3건 포함), v3 4파일은 직독, 그림자 CSV 5일치는 PowerShell 전수 집계.
- 출처 태그: [실측]=코드 직독·데이터 집계·재현 실행으로 확인 / [추정]=근거 있는 예상, 미확인.

---

## 0. 결론 (3줄)

1. **내일 LIVE로 켜는 ROCKET 레인은 그림자 5일간 발동 0건** [실측] — 한 번도 안 울린 레인을 실전 첫 실행하는 것이며, 엔진 내부 v3 배선도 shadow로는 예행 불가 구조라 검증이 재생(replay) 하나에 전부 걸려 있다.
2. **8/18 10전 10패의 진범(진입 3~5초 방어매도 = 스톱이 노이즈 안)은 v3가 하나도 안 고쳤다** [실측] — v3는 진입만 바꿨고 매도는 공용 엔진 그대로. ROCKET은 그 사고 구간(09:00:00~01:00) 한복판에서 사며, 섹터 집중·진입 페이싱 무방비도 8/18 구조 그대로다.
3. **조용사 취약 구조가 반복 패턴**: 현재 디스크 상태는 레거시·v3 양쪽 다 닫힘(내일 플립 누락 시 S01 무음 정지, 알림 장치 0) + 데이터 정전 시 매도판정 전면 정지(하드스톱 포함) + 상태파일 손상 시 방어장치 무음 해제 — 전부 "죽어도 아무도 모르는" 계열이다 [실측].

---

## 1. 🔴 내일(8/28~29) 실전 연결 전 반드시 볼 것

### 1-1. 현재 런처는 양쪽 경로 모두 닫힘 — 플립 누락 = S01 무음 전면 정지 [실측]
- `RUN\hidden\SAFEPLUS_STRATEGY01_LIVE.cmd`·`..._SIGNAL.cmd` 둘 다 지금 `S01_LEGACY_ENTRY_LIVE=NO` + `S01_ENTRY_V3_MODE=SHADOW`.
- 계약 로직(strategy_01_signal_contract_v2.py:204-207): 이 조합 = `source_rows=[]` = 신호 0건. **8/28 당일 S01은 매수 0건이 정상 동작**(캡처만 하는 날이면 의도일 수 있으나, 의도인지 친구님 확인 필요).
- **"양쪽 다 꺼짐"을 알리는 이벤트·로그가 계약에도 엔진에도 없다** — 빈 배열은 "장이 조용함"과 구분 불가. 8/29 아침 첫 확인 항목은 "신호가 소비되는가"여야 한다.
- 파생 함정: 모드 문자열 오타("ON"·"live " 등)는 무검증 SHADOW 강등(contract:23-25) → 같은 무음 정지.

### 1-2. ROCKET 레인: 그림자 5일간 확정 0건 [실측 — shadow CSV 전수]
| 날짜 | ROCKET | PULLBACK | 총 행 |
|---|---|---|---|
| 8/21 | **0** | 0 | 28,665 |
| 8/24 | **0** | 15 | 17,999 |
| 8/25 | **0** | 1 | 15,940 |
| 8/26 | **0** | 32 | 25,815 |
| 8/27 | **0** | 5 | 10,139 |

- 8/27 ROCKET 창(09:00~09:01) 479행 전수: `auction_price_rising`·`first_5s_high_break`·`auction_buy_ratio≥0.65` **셋 다 479/479 실패(100%)**. 8/26(활황일)도 개별 통과율 14%/17%/30%인데 13개 조건 AND는 한 번도 안 겹쳤다.
- 구조 원인 [실측]: 호가·기대체결 deque는 09:00 이전만 쌓인다(entry_runtime_v3:58) → ROCKET 창 내내 `auction_rising`·`auction_buy_ratio`는 **개장 전 마지막 값으로 동결**된 정적 조건. 장 시작 후 아무리 좋아져도 안 변한다.
- 판단: 실전 위험은 낮다(안 사니까). 진짜 위험은 ①**처음 발동하는 날이 곧 첫 실전**이고 ②3슬롯을 배정했는데 영구 0건이면 슬롯 낭비라는 것. 조건 완화는 재생 검증 후 별도 안건.

### 1-3. 엔진 v3 배선은 shadow로 예행 불가 — 내일이 사실상 첫 실행 [실측]
- S01 shadow면 `buy_allowed=False`(approval_settings_guard.py:607-613)가 신호 선별 **이전에** 차단 → 엔진 내부 ROCKET/PULLBACK 슬롯 계산·reference_low 배선은 한 번도 실행된 적 없는 경로다. 검증은 재생 PASS 하나에 전부 의존.

### 1-4. v3 신호 ID 충돌 [실측]
- v3 행에 `signal_sequence` 없음 → signal_id = `day:code:0:ts`. 같은 초에 같은 코드가 ROCKET·PULLBACK 동시 발행되면 ID 충돌 — 한쪽 소비가 다른 쪽을 소각(entry_runtime_v3:189-194 + contract:175-179).

### 1-5. 제3의 통로 S01_ROCKET_LIVE(현재 NO) [실측]
- 켜지면 ROCKET 1건이 **레거시 signals 채널**로 승격(signal v2:1465-1471). "레거시 정지" 의도와 어긋나는 우회로가 남아 있다. 참고: 승격 경로는 하루 1건 상한·PULLBACK 승격 없음이지만, 내일 LIVE 모드는 entry_v3_signals 통째 소비라 이 상한과 무관하다.

---

## 2. 🔴 8/18 사고 구조가 그대로인 것 (트레이딩 관점)

1. **매도 3~5초 방어매도(10전 10패의 진범) 미수정** [실측] — 엔진은 board_fresh면 매 tick 공용 매도엔진(strategy_common_hold_sell_v1)에 그대로 위임(engine:2247). 진입 직후 노이즈 필터는 어디에도 없다. **v3로 진입이 좋아져도 팔리는 방식이 같으면 결과가 같을 수 있다.**
2. **ROCKET은 사고 구간 한복판에서 산다** [실측] — 창이 09:00:00~09:01:00(policy_v3:112), 8/18 체결 경계(09:00:10~40)와 정면 겹침. 신호기의 09:03 지연 실험은 shadow 전용에 관찰 날짜도 만료(signal v2:74-77).
3. **섹터 집중 무방비** [실측] — 엔진·계약·v3 정책 전부에 섹터/테마 상한 0. 레거시 정렬은 theme_bonus 1순위라 오히려 집중 가중(contract:41-48). 8/18 "6개 전부 반도체" 재발 가능.
4. **진입 페이싱 없음** [실측] — 한 tick에 후보 수만큼 연속 매수 제출 가능(engine:1339), ROCKET 3슬롯은 같은 3초 배치에서 동시 소진 가능(policy select_batch가 배치당 3 허용). open_priority의 3초 페이싱은 SHADOW라 미작동.
5. **손실 연쇄 방어 반쪽** [실측] — 2연패 5분 정지(entry_v3_loss_pause)는 v3 선별만 막고, 이미 발행된 TTL 5초 신호는 뚫는다. 손실재진입 게이트도 SHADOW. 6종목×2사이클=최대 12연패 구조는 열려 있음. 게다가 손익 판정이 슬리피지 이전 값(signal v2:285,306)이라 8/18형(슬리피지가 본체) 손실엔 발동 안 할 수 있다 [추정].

---

## 3. 🟠 데이터 정전·손상 시 위험한 방향으로 넘어지는 곳 (fail-open)

| # | 위치 | 내용 | 태그 |
|---|---|---|---|
| 1 | engine:2229-2235 | **board 낡으면 매도판정 전면 정지(하드스톱 포함)** — 폭락해도 15:10 강제청산까지 무매도, 경보 0 | [실측] |
| 2 | engine:98-102 | `number()`가 숫자 0을 기본값으로 오염(`0 or ""` 관용구) → 개장 직후 정당한 누적 0이 "자료 없음(-1)" 처리 | [실측·재현] |
| 3 | engine:2044 | 자금흐름 결손 시 buy_ratio를 **0.60(매수우위)으로 날조** → 결손일수록 매도 미룸 | [실측] |
| 4 | engine:88-95 | ts 파싱 실패 시 fallback=now → 낡음 검사(4s/8s) 무력화, 낡은 가격으로 판정 | [실측] |
| 5 | engine:513-527 | 상태파일 손상 시 빈 상태로 기동 → 일일 6종목 한도·신호 중복소비 방어 소멸 | [실측] |
| 6 | signal:1433-1437 | rotation state 읽기 실패 무음 → adaptive confirm·loss pause 등 8/18 방어 전부 조용히 해제 | [실측] |
| 7 | signal:419-428 | **TOP40 fail-open 안전장치 무력화** — 고저폭판만 죽고 trend판 살면 병합 탓에 "제한 없음"이 아니라 **전 종목 탈락**(주석 의도와 정반대) | [실측, trend판에 hr_rank 없음은 추정] |
| 8 | 전 소비 사슬 | **source_stale 아무도 안 봄 확정** — 원천은 깃발을 싣는데 엔진·계약·신호원 전부 무시. 낡은 일봉의 TOP40으로 그대로 매수(8/13 기록의 수리 후보가 아직 미수리) | [실측] |
| 9 | engine:2208-2217, 483 | 데이터 정전 알림 채널 자체가 없음 — `_last_data_warning`은 쓰기만 있는 죽은 속성 | [실측] |
| 10 | signal:541-542 | 역전/동가 호가를 스프레드 0으로 보고 → 데이터 오류가 35bps 게이트를 최상 조건으로 통과 | [실측] |

## 4. 🟠 프로세스 생존성

1. **`_event()` CSV append 무가드 — 엔진 통째 사망 경로** [실측] (engine:846-852). 장중 이벤트 CSV를 Excel로 열면 PermissionError → run()까지 전파 → SELL_PENDING 방치. 바로 옆 `_order_lifecycle`(802-809)은 가드 있음.
2. **ProcessLock PID 재사용 오인 + exit 0** [실측·재현] (engine:355-363, 2876). 크래시 후 락 PID를 무관 프로세스가 쓰면 "already running"으로 성공 종료 — 개장 내내 엔진 부재를 아무도 모름.
3. **자정 롤오버 없음** [실측, 야간 생존은 추정] (engine:2856-2866). RECOVERY_BLOCKED 보유로 밤을 넘기면 익일 전일 상태로 진입창 진입 — 전일 6종목이면 익일 무음 전면 차단.
4. **신호기 `_append_events` 매 호출 전체 CSV 재독** [실측, 지연 크기는 추정] (signal:1107-1121). 창 후반으로 갈수록 tick 지연↑ → 2초 넘는 순간 confirm 리셋 반복 = **후반부 신호 기아**.
5. 기동 시 해시 대상 파일 1개라도 없으면 무방비 크래시 루프 [실측] (signal:1423-1426).

## 5. 🟡 로직 결함 (판정이 틀리는 곳)

1. **저점 기준이 "개장 후 저점"이 아니라 약 5.4분 롤링 저점** [실측] — samples deque maxlen=180 × 실측 배치주기 1.81초 ≈ 326초. 09:02에 -5% 급락한 종목이 09:10엔 그걸 잊고 "-1% 눌림"으로 PULLBACK 자격 획득 가능. dip·rebound·low_stable_sec·higher_low_pct 전부 오염 (entry_runtime_v3:16,61-64).
2. **시가 첫 틱 래치** [실측] (signal:628-629) — 첫 틱이 +2% 지점이면 그 값이 종일 시가 행세 → 눌림 깊이 띠(-0.5~-3%) 전부 오판.
3. `buy_accelerating`이 표본 5~10초 구간에서 자동 참 [실측] (entry_runtime_v3:116) — 가속 검증이 정작 ROCKET 창 초반에 무의미.
4. rising_sec이 보합을 상승으로 계산 [실측] (signal:1001-1006) — 1틱 오르고 5초 보합이면 "5초 상승 지속" 통과.
5. confirm_ticks 리터럴 2 매초 덮어씀 [실측] (signal:1439) — 상수 3으로 올려도 무효.
6. adaptive(09:00~09:20 청산만)와 pause(당일 전체)의 "2연패" 정의 불일치 [실측] (signal:281 vs 303-305).
7. 부분체결 시 fill_wait 생략 [실측] (engine:1879-1881) — 현행 1주에선 무해, 수량 증대 시 조기취소 버그.
8. `_ma3_entry_trend_ready`가 감사기록 reason 오염 [실측] (signal:793-794) — action과 reason이 자기모순.
9. 발행 재시도 10초 간격 + 신선도 5초 = 5~10초 소비 사각지대 반복 [실측] (runtime:26,180-186 + contract:218) — 엔진 루프가 잠깐만 막혀도 신호 증발. **8/19 S03 미소비 사고와 같은 계열이며 그 사고 자체도 아직 미수리**(조사 지시서만 있음).

## 6. ⚪ 죽은 코드·모순 (오독 위험, 동작 무해)

- `emitted` set: 추가만 하고 아무도 안 읽음 (runtime:30,187) [실측]
- `entry_v3_mode:"SHADOW"` 하드코딩 (signal:1498) — 내일 LIVE여도 산출물엔 영원히 SHADOW 표기, 사후 판독 오판 유발 [실측]
- theme_leader 죽은 배선: 항상 False → 테마 가산·왕관 표시 전부 사문 (signal:188,649) [실측]
- `_direct_hard_stop_permitted` 항상 True·호출자 없음 — 이름이 하드스톱 우회를 암시해 위험한 오독 소지 (engine:2142-2147) [실측]
- `_try_staged_add`(STRONG_FLOW 증량): 내일 레거시 정지 후 영구 도달 불가 (engine:1131-1272) [실측]
- 만료 실험 코드(above-open LIVE·09:03 shadow)가 매 tick 계속 실행 (signal:65-77,1379) [실측]
- `S01_MAX_CYCLES_PER_CODE` env: 2 외 값이면 부팅 크래시 — 설정이 아니라 지뢰 (signal:150 vs 560-562) [실측]
- mode 라벨 `SIGNAL_ONLY_ORDER_ZERO`·HTML "실계좌 주문 기능 없음" 배너 vs 실제 실주문 — 안전감 오도 (signal:1079,1206) [실측]
- 중복 cycles 검사 사문 (engine:1399-1406) · `_recent_3min_high` 호출자 없음 (engine:1944-1953) [실측]

## 7. 설계 관찰 (버그 아님, 친구님 판단 사항)

- **PULLBACK은 시가 아래 눌림(-0.5~-3%)만 잡는다** — 시가 위에서 급등→눌림하는 고전 W자는 자격 없음(policy_v3:129). 의도라면 OK, 아니면 레인 사각.
- ROCKET 3슬롯이 같은 3초 배치에서 동시 소진 가능 — 시간 분산 없음.
- 점수 동점 시 종목코드 내림차순이 승자(policy_v3:151) — 임의적이나 결정적.
- 상대거래량 기준선은 09:03 전엔 결손 무처리 → ROCKET 점수가 호가·수급 쪽으로 쏠림(무해).

---

## 부록: 검증 증거

- 그림자 집계 명령·결과: 이 세션 실측 (Import-Csv 전수, 8/21~8/27 5개 파일 98,558행)
- 배치 주기: 8/27 09:00~09:20 상위 3코드 각 660샘플/1,195초 = 1.81초/샘플
- `entry_v3_signals` 배선 정상(signal:1504 ↔ contract:205), 그림자 CSV 기록기는 열 합집합+extrasaction=ignore로 S06형 사망 방어됨(signal:1097-1129, 7/29 수리) — 안심 확인 2건
- 엔진 검토는 재현 스크립트 3건(scratchpad, 원본 무수정): number(0) 오염·os.kill(pid,0) 동작·도달불가 분기
