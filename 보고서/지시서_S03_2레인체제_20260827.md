# 지시서 — S03을 계단·급락 2레인 체제로 정리

작성 2026-08-27 10:30 · 지시자 친구님 · 작성자 Claude(중계 세션)
집행 대상: **다른 Claude 세션**
※ 이 문서가 `지시서_S03_급행제거_20260827.md` 를 대체한다(수정 3건을 한 번에 묶음).

---

## 0. 친구님 지시 원문

> "EARLY_LOW 시간창 마감 후 고쳐 이거는 지금 껏어 앞으로 사용안할거야
>  급락 하고 계단식으로 내려오는거 두개만 있어야돼 나머지는 있으면 안돼"
> "급행도 꺼 두개만 남겨"
> "2번으로 해 급행 끄고 822행도 없애"
> "지금 수정 할꺼 있으면 수정해야돼"

**최종 목표 상태**
```
S03 레인 = 계단(OPEN_CRASH) + 급락(INTRADAY_CRASH)  두 개만
폐지      = EARLY_LOW · EXPRESS(급행) · FLOW_TURN_FAST
```

---

## 1. ⚠️ 먼저 읽을 것 — 장중 수정이다

- 지금 **장중**이고 `CLAUDE.local.md` 규칙 1은 08:40~15:30 실전파일 편집 금지다.
  친구님이 그 자리에서 명시 지시했으므로 예외 집행한다.
- 파일을 고쳐도 **떠 있는 프로세스에는 즉시 반영되지 않는다.** 위험은 **재기동 순간**이다.
  ⇒ **테스트를 통과 못한 중간 상태로 남기지 말 것. 한 번에 끝내거나 원복한다.**
- **2레인(INTRADAY_CRASH)은 09:20~14:30 현재 활성이다. 이 레인의 판정·문턱·시간창을 바꾸지 말 것.**
- 수정을 오늘 실전에 반영하려면 S03 신호기·엔진 재시작이 필요하다.
  **재시작 여부는 친구님께 별도 확인할 것.** 임의 재시작 금지.

---

## 2. 수정 대상 (실측 행번호 — 10:30 기준. 편집 직전 반드시 현재 내용 대조)

### A. `RUN\골짜기_급반등.py`

**A-1. 822행 — 깊은 구역 차단 제거 ★2안 핵심**
```python
# 현재 (주석 2줄 + if 2줄 = 4줄)
        # ★[S03-EXPRESS 2026-08-06] 종전 인계선(-8%) 아래에서 4단계 경로로 쏘면
        #   신호 검사기(-8% 하한 유지)가 버려서 총알만 소진된다 — 깊은 곳은 급행 전용.
        if point.price <= point.open_price * (1.0 + self.config.handoff_drop_pct / 100.0):
            failures.append("DEEP_ZONE_EXPRESS_ONLY")
```
→ 4줄 삭제. 자리에 사유 주석을 남길 것:
`# ★[2026-08-27 친구님 지시] 급행 폐지 — 깊은 구역도 계단이 직접 잡는다.`

**A-2. 697~725행 부근 — 급행 매수 발행 경로 삭제**
- `BUY_READY` 사유 `"S03_EXPRESS_DEEP_CRASH+SELL_DECEL+BUY_FLIP"` 를 반환하는 블록 전체
- 주석 `★[S03-EXPRESS 2026-08-06 친구님 지시 "칼날처럼 떨어질 때…"]` 로 시작하는 구간
- ⚠️ 정확한 시작·끝 줄은 편집 직전 재확인

**A-3. 1584행 — EARLY_LOW 시간창 (승인 범위 위반 수정)**
```python
# 현재
                    EARLY_LOW_CAPTURE_START <= point.ts.time() < ENTRY_END
```
명부 승인은 `S03_EARLY_LOW_0900_0910_...` = **09:00~09:10** 인데 코드는 `ENTRY_END`(14:30)까지 열려 있다.
오늘 실제로 09:16:56·09:17:20 두 건이 승인 창 밖에서 발행됐다.
→ EARLY_LOW 자체를 폐지하므로 **이 조건문이 속한 EARLY_LOW 발행 블록을 통째로 제거**하는 것이 본 지시다.
   블록 제거가 위험하다고 판단되면 최소 조치로 `ENTRY_END` → `EARLY_LOW_CAPTURE_END` 로 바꾸고 보고할 것.

**A-4. import 정리 (52~55행)** — A-5 결정 후 **미사용분만** 삭제
```python
    EXPRESS_DEPTH_PCT,
    EXPRESS_FAST_DROP_PCT,
    EXPRESS_FAST_WINDOW_SEC,
    EXPRESS_NEAR_LOW_PCT,
```

> 🚫 **`EXPRESS_FAST_WINDOW_SEC` 은 지우면 안 된다.** 464행에서 `flow_points` 보관 창(600초)으로도 쓰인다.
> ```python
>         cutoff = point.ts.timestamp() - EXPRESS_FAST_WINDOW_SEC
> ```
> 급행 상수라고 같이 지우면 **흐름 데이터 보관이 깨진다.** 이름을 `FLOW_KEEP_WINDOW_SEC` 로 바꾸거나
> 그대로 두고 용도 주석을 달 것. 값 600은 유지(기존 소비자 `_pre_rates` 180초·`_flow_acceleration` 20초는 자기 창만 봄).

**A-5. 677~690행 `express_deep` — 판단 필요 (3절)**

### B. `RUN\strategy_03_signal_contract_v1.py`

**B-1. 325~332행 — 급행 신호 검사 분기 삭제**
```python
    if str(raw.get("reason") or "").startswith("S03_EXPRESS"):
        return (
            algorithm == OPEN_CRASH_ALGORITHM
            and in_window
            and open_price > 0
            and float(raw.get("express_depth_pct") or 0) <= EXPRESS_DEPTH_PCT
            and 0.0 <= rebound <= EXPRESS_NEAR_LOW_PCT
        )
```
→ 블록 전체 삭제

**B-2. 337행 — 계단의 −8% 하한 제거 ★2안 핵심**
```python
# 현재
        and OPEN_HANDOFF_DROP_PCT < drop_from_open <= OPEN_ARM_DROP_PCT
# 변경
        and drop_from_open <= OPEN_ARM_DROP_PCT
```
> ⚠️ **A-1과 B-2는 반드시 같이 간다.** A-1만 하면 신호검사기가 버려서 총알만 소진되고,
> B-2만 하면 822행이 막아서 아무 변화가 없다.

**B-3. 상수 정리 (27·34~37행)**
`OPEN_HANDOFF_DROP_PCT = -8.0` 은 A-1 후 822행에서 안 쓰이지만
**`골짜기_급반등.py` 107행 `handoff_drop_pct` 와 121~122행 검증문이 아직 참조**한다.
```python
        if not self.handoff_drop_pct < self.arm_drop_pct < 0:
            raise ValueError("drop range must satisfy handoff < arm < 0")
```
**상수만 지우면 터진다.** 이번 범위에서는 **상수를 남겨둘 것**(잔재 정리는 3절 ② 판단).

### C. `RUN\hidden\SAFEPLUS_STRATEGY03_LIVE_ASCII.cmd`

```
5행   set S03_EARLY_LOW_LIVE=AUTO       → NO
6행   set S03_FLOW_TURN_FAST_LIVE=YES   → NO
```
> 🚫 **`.cmd` 는 CRLF·ASCII 전용.** Edit 도구는 줄끝을 LF로 바꾸므로 쓰지 말 것.
> 파이썬 바이너리 모드로 편집하고, 편집 후 `CR 개수 == LF 개수` 와 ASCII 여부를 검증할 것.
> **이 파일은 승인 명부·관리자 해시 양쪽에 있다 → 재봉인 필수.**

### D. 명부 `config\live_approved_hashes_v1.json`

```
live_features.S03_FLOW_TURN_FAST : true → false
live_features.S03_EARLY_LOW      : 이미 false (오늘 09:10 다른 세션이 처리, 손대지 말 것)
```
+ A·B·C 파일들의 sha256 재봉인
+ `approval_scope` 에 이번 변경 사유 1건 추가

**반드시 `RUN\approval_manifest_writer_v1.py`(CAS)로만.**
템플릿: `RUN\update_broker_s01_manifest_20260826.py`
`config\elevated_path_hashes.json` 도 C 파일을 담고 있는지 확인하고 같이 갱신할 것.

---

## 3. 판단이 필요한 지점 — 임의로 정하지 말 것

### ① `express_deep` 무장 조건 (골짜기_급반등.py 677~690)
```python
            express_deep = (
                state.day_high > 0
                and (point.price / state.day_high - 1.0) * 100.0 <= EXPRESS_DEPTH_PCT
            )
            if point.price > arm_price and not express_deep:
                return self._row("WAIT", "OPEN_DROP_GT_4PCT", point, profile)
```
이건 **매수가 아니라 "저점 추적 시작(무장)" 조건**이다.
원 주석 실증: *7/29 376900 시가 28,000 → 33,500 급등 → −15% 폭락 — 시가 기준으론 무장 불가*

- **유지(권고)**: 시가 위에서 급등 후 폭락한 종목도 무장 → 822행이 사라졌으니 **계단이 직접 잡는다.**
  2안 취지와 맞는다. 이름만 `deep_arm` 등으로 변경.
- **제거**: 시가 −4% 밴드 종목만 무장. 7/29 376900 유형은 놓친다.

**⇒ 권고는 "유지 + 이름 변경". 친구님 확인 후 집행.**

### ② `handoff_to_s06` 잔재 정리를 이번에 같이 할지
373·446~447·620·1087·1093행. 판정에 안 쓰이는 이름뿐인 유산이며
친구님이 *"이런 역할이 여기 있으면 안 되는 거 아니니?"* 라고 지적한 대상.
**권고: 이번엔 손대지 말고 마감 후 별건.** 장중 diff를 키우지 않는다.

---

## 4. ⚠️ 급행 제거의 부작용 — 알고 집행할 것

**급행은 "당일 고점 대비"를 보는 유일한 경로다.**

| 레인 | 기준 |
|---|---|
| 계단 OPEN_CRASH | **시가** 대비 −4% ~ (−8% 하한은 B-2로 제거) |
| 급락 INTRADAY_CRASH | 장중 고점 대비 −5% + 반등 1.0~1.5% |
| 급행 EXPRESS(폐지 대상) | **당일 고점** 대비 −7% |

**10:20 실측 — 갭 하락 사각지대가 실재한다:**
```
종목      전일대비   시가대비     갭      고점대비
243070    -8.67%    -1.95%    -6.85%    -3.21%
257720    -6.56%    +0.31%    -6.85%    -1.73%
```
243070은 오늘 최대 낙폭인데 낙폭의 −6.85%p가 갭에서 났다.
시가 대비로는 −1.95%라 **계단·S06 어느 쪽도 못 잡는다.**
급행마저 없애면 이 유형은 완전 사각지대가 된다.

친구님은 이 설명을 듣고 2안(급행 제거 + 822행 제거)을 선택하셨다. 그대로 집행한다.

---

## 5. 금지 사항

- A~D 외 **다른 전략(S01·S02·S04·S05·S06) 칸을 건드리지 말 것.**
  공용 파일이면 대상 전략 칸만. diff에 이웃 칸이 보이면 그 자리에서 중단·보고.
- **2레인 판정·문턱·시간창 변경 금지** (현재 활성)
- 매도 규칙·수량·슬롯·자본·하드스톱·강제청산 변경 금지
- 리팩토링·포맷 변경·파일 신설 금지. 요청된 것만.
- PowerShell 로 JSON 쓰기 금지. 명부는 CAS writer 로만.
- **S03 프로세스 임의 재시작 금지** (친구님 확인 후)

---

## 6. 절차 (순서 고정, 건너뛰기 금지)

1. **테스트 기준선 먼저 측정** — `tests\test_strategy_03_*` 실행해 **수리 전 실패 개수를 기록**
   (기존 실패에 새 실패가 숨는다 — 8/5 실사고)
2. **백업 3개**
   ```
   RUN\backup\골짜기_급반등_20260827_before_2lane.py
   RUN\backup\strategy_03_signal_contract_v1_20260827_before_2lane.py
   RUN\backup\SAFEPLUS_STRATEGY03_LIVE_ASCII_20260827_before_2lane.cmd
   RUN\backup\live_approved_hashes_v1_20260827_before_2lane.json
   ```
3. **편집 전 현재 내용 대조** (행번호는 10:30 기준)
4. 수정: A-1 → B-2 → A-2 → B-1 → A-3 → C → A-4 (A-5는 확인 후)
5. **컴파일** — `compile()` 로 .py 2개 / `.cmd` 는 CR·LF 개수 + ASCII 검증
6. **테스트 재실행 → 1번 기준선과 대조표 제출**
7. **해시 재봉인** (승인명부 + elevated 양쪽, 이상 0 확인)
8. **검증자 호출** — `stockbot-verifier` 에이전트로 PASS/FAIL
9. 완료 보고 (7절 형식)

---

## 7. 완료 증거 형식

```
수정 파일 / 변경 줄 수 (diff 요약)
컴파일 결과 (.py 2개) / .cmd CR·LF 개수 + ASCII 여부
테스트: 수리 전 N failed → 수리 후 M failed  (대조표)
해시 재봉인: 승인명부 X건 이상 0 / elevated Y건 이상 0
A-5 판단 결과와 근거
백업 경로 4개
재시작 여부 (했다면 누구 승인으로)
```

---

## 8. 롤백

3회 시도해도 테스트가 통과하지 않으면 **원상복구 후 보고.**
**실패한 채로 방치 금지 — 장중이라 재기동 시 그대로 실전에 뜬다.**

---

## 9. 이번 범위 밖 — 마감 후 별건 안건

1. **`handoff_to_s06` 잔재 정리** (3절 ②)
2. **유니버스 설계** — 오늘 10:25 실측:
   ```
   2레인 매수구간(고점대비 -5%↓ + 반등 1.0~1.5%) 8종목 중
     감시망 안 2개 / 감시망 밖 6개 (75%)
   ```
   유니버스를 **전일 변동성**(고저폭 10%+·거래대금 100억+)으로만 뽑아서,
   어제 조용했으면 오늘 감시망이 얇아지고 오늘 급락이 나와도 못 본다.
   ※ 오늘 고저폭판 14건은 **코드 문제가 아니다** — 8/25 114종목 → 8/26 38종목으로
     시장 변동성이 1/3로 준 탓이며 통과율은 36%→37%로 동일하다.
3. **S02 신호기 첫 산출 지연** — 09:00 시작 후 첫 저널까지 40분 넘게 걸렸다(어제까진 정상)

---

## 10. 참고 — 오늘 S03 현황 (10:30 실측)

```
신호      S03_EARLY_60S_REBOUND_V1  20건 (09:00:19~09:17:20, 09:10 이후 2건은 승인창 밖)
          S06_STAIRCASE_RETEST_V1    5건 (09:09:21~09:14:42, 정상 창)
          S03_INTRADAY_CRASH_REBOUND_V1  0건
2레인 후보 49건 탈락사유
          INTRADAY_DRAWDOWN_LT_3PCT   25
          DUPLICATE_OR_OLD_SNAPSHOT   20
          CODE_DAILY_ENTRY_LIMIT_2     2
          DEAD_LOW_REQUIRES_LOWER_LOW  2
당일종목  S03 5/6 (한도 거의 소진) · S01 2/6
EARLY_LOW 주문은 09:10 명부 차단으로 이미 막혀 있음 (live_features=false)
```
