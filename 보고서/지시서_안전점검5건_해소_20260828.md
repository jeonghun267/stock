# 지시서 — 안전점검 5건 해소 (번호순 · 범위 엄수)

작성 2026-08-28 밤 · 승인자 친구님 · 작성자 Claude(검증자 겸임) · 집행자 GPT
클로드 실측 검증: 5건 전부 사실 확인됨. **4번은 클로드가 이미 해결**(추세판 태스크
15:35 → 17:30 이동 완료) — 이 지시서는 1·2·3·5번만 다룬다.

## ⛔ 범위 절대 규칙 (친구님 지시 "다른 거 건드는 거 있으면 안 돼")

- 아래 명시된 파일·상태·태스크 외에는 **어떤 것도 만지지 마라.**
- 특히 금지: 전략 매수·매도 조건, S01/S03 오늘 배선된 그림자들, 브로커 주문 경로,
  종가매수 실행기(동결 중), 런처 env, 승인 명부의 다른 항목.
- 커버 파일을 만지게 되면 재봉인 한 세트. 막히면 중단·보고.

## 1. [최우선 — 월요일 08:55 전] S06 복구 차단 해소

현상[실측]: `strategy_06_crash_low_chase_state_v1.json` 의 `recovery_blocked=True`,
`last_error=STATE_SAVE_FAILURE_RECONCILE_REQUIRED` — 이대로면 월요일 S06 매수 전면 차단.
1. 상태 저장 실패의 원인(파일 잠금/권한/디스크)을 로그로 규명해 보고.
2. 브로커 체결·잔고 기록과 상태파일 포지션을 대조해 유령/누락 0을 확인.
3. 대조 일치 확인 후에만 `recovery_blocked=False` 로 정리(상태파일 수정 —
   코드 수정 아님). 대조 불일치 발견 시 **정리하지 말고 중단·보고**.
4. 증거: 대조표 + 정리 전후 상태파일 diff + S06 집중 테스트 통과.

## 2. 1분봉 공백 보강

1. 수집기(collect_prices_1m)의 지연·공백 원인(과다 구독·재연결)을 로그로 규명해 보고.
2. 수정은 **수집기 계열 파일만**(전략 무접촉). 구독 분할·재연결 보강.
3. 증거: 최근 3거래일 공백률과 수정 후 기대치, 컴파일·해당 테스트.
   ⚠️BATCH_TR 규칙: 대량 재조회 로직을 넣더라도 15:15~15:26 창과 겹치지 않게.

## 3. 실패 태스크 2건 수리

현상[실측]: `SAFEPLUS_STRATEGY05_LIVE` Last Result **-1**, `SAFEPLUS_EMERGENCY_STATUS`
Last Result **1**.
1. 각 태스크의 실행 명령·경로·권한·종료코드를 규명(READ-ONLY 수동 실행으로 재현).
2. 수정은 해당 태스크의 실행 대상 스크립트/명령줄만.
3. 증거: 수동 실행 종료코드 0 + 실패 원인 1줄 설명. **태스크 시각·트리거 변경 금지.**

## 5. Git 미커밋 189건 + 재생파일 35GB 정리

1. Git: 커밋은 하지 말고, **분류 보고만** — 명부 승인본과 일치하는 변경 / 불일치
   변경 / 산출물성 파일 3분류 목록. (커밋 여부는 친구님 별도 결정)
2. 35GB 재생파일(`s01_entry_v3_exact_replay` 등): **삭제 금지.** 보존기간·용량상한
   정책안만 제시(예: 14일 보관 후 압축, 상한 50GB). 실행은 친구님 승인 후.
3. 증거: 분류표 + 정책안 1쪽.

## 완료 보고 형식

번호별로: 원인 1줄 / 수정 파일·줄 범위(또는 "보고만") / 증거 / [UNVERIFIED] 표기.
보고 후 클로드가 전수 검증한다(오늘 밤 기준선 확보됨).
