# 지시서 — 종가매수 재생 게이트 완화 (이중 자물쇠 해소)

작성 2026-08-28 저녁 · 승인자 친구님("지시서 내려 → 고") · 작성자 Claude(검증자 겸임) · 집행자 GPT
대상: `RUN\eod_gap_live_executor_v1.py` 의 `_replay_entry_audit()` (1096행~) 한 함수

## 0. 왜 (8/28 실측)

- 1148행 `eligible < 1 → raise`: 전일에 주문자격 후보가 없으면 재생 FAIL → LIVE 불가.
  후보는 하루 평균 0.78개라 "후보 있는 날 이틀 연속"을 요구하는 현 구조에선
  실매수가 사실상 발생 불가. **검증의 본질은 '판정이 재현되는가'이지
  '후보가 있었는가'가 아니다.**
- 1106행 해시 불일치 FATAL: 동작은 옳으나(코드 바뀌면 옛 감사 재생 무효)
  메시지가 사유 구분 없이 죽어서 운영자가 원인을 매번 재조사한다.

## 1. 원하는 결과

1. **[핵심] eligible=0 이어도 판정 재현이 전건 일치하면 PASS.**
   - 1148~1149행의 `if eligible < 1: raise` 를 제거하고, 리포트에
     `"eligible_candidates": <수>` 를 기록한다 (0이면 0으로 정직하게).
   - 나머지 검사(해시·config·관측 완결성·판정 재현 전건 일치)는 전부 그대로.
2. **[보조] 불일치 사유의 구분 로그.** 해시/설정 불일치 시 raise 는 유지하되
   메시지를 구분한다:
   - 해시: `ENGINE_CHANGED_REBASELINE: 오늘 SHADOW 캡처부터 재기준, 익일 자동 회복`
   - 설정: `CONFIG_CHANGED_REBASELINE: <키>` (동일 문구 체계)
   동작(당일 LIVE 불가)은 바꾸지 않는다 — 바뀐 코드로 옛 감사를 통과시키는 완화는 금지.

## 2. 반드시 지킬 제약

- 수정 범위: `_replay_entry_audit` 함수와 그 메시지 문자열만.
  **점수·구간·주문·pick 흐름·다른 게이트(경매 재생 게이트 포함) 일절 무접촉.**
- 수정 전 백업: `RUN\backup\eod_gap_live_executor_v1_20260828_before_replay_gate_relax.py`
- 명부/기준표 커버 밖 재확인 후 진행 (8/28 실측: 파일 등록 없음 — 재확인만).
- 테스트: 기존 eod_gap 15개 통과 유지 + **신규 1개** —
  "판정 전건 재현·eligible=0 → PASS" 케이스 (tests\test_eod_gap_entry_safety_v1.py 에 추가).
- 실행 프로세스 재시작 금지. [UNVERIFIED] — 다음 거래일 15:17 이 첫 실측.

## 3. 완료 증거

1. diff (한 함수만 변경인지 줄 범위 명시)
2. py_compile OK + 테스트 16개(15+1) 통과
3. 8/28 감사로 드라이런: `ENGINE_CHANGED_REBASELINE` 사유가 새 문구로 찍히는지
   (오늘 감사는 구코드 해시라 이 경로가 정상 작동 증거가 된다)
4. 명부 커버 밖 확인 출력

## 4. 이 지시서 이후의 시간표 (예상)

- 월(9/1) 15:17: 해시 불일치 → `ENGINE_CHANGED_REBASELINE` (오늘 수정 탓, 마지막 1회)
  → SHADOW 캡처 (새 해시·새 config)
- 화(9/2) 15:17: 재생 PASS (후보 0이어도) → **LIVE 첫 가동 가능**
  → 그날 60~70 구간·1만원·시총 통과 후보가 있으면 **사상 첫 종가매수 주문**
