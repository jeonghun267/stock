# GPT 수리 지시서 — 2026-08-26 S06 관련 2건

작성: Claude / 작성시각 2026-08-26 12:20 (장중, 실전 가동 중)
대상: `C:\stock_bot`
**전체 원칙: 아래 2건 외에는 어떤 파일도 열지 말 것. 다른 전략(S01·S02·S03·S05)·공용매도·돈맥은 건드리지 말 것.**

---

## ⛔ 공통 제약 (반드시 지킬 것)

1. **장중(08:40~15:30) 실전파일 편집 금지.** 이 지시서의 수리는 **15:30 이후**에 착수한다.
2. `.cmd` 파일은 **CRLF + ASCII 전용**. 편집 후 반드시 CR 개수와 비ASCII 개수를 대조한다.
   - ⚠️ 실측 교훈(오늘): `sed -i` 는 .cmd 의 CRLF 를 LF 로 바꾼다(2222→2179바이트). **바이트 단위 치환**으로만 편집할 것.
3. `.py` 도 이 저장소는 CRLF 다. 줄끝을 바꾸지 말 것.
4. 수정한 파일이 `config\live_approved_hashes_v1.json` 의 명부에 있으면 **반드시 재봉인**한다.
   - 도구: `RUN\approval_manifest_writer_v1.py` 의 `update_manifest(mutate, updated_by=..., expect_sha=read_content_sha(MANIFEST))` (CAS 방식)
   - 재봉인 후 전체 63개 해시가 일치하는지 확인한다(common 18 / S01 11 / S02 7 / S03 14 / S06 13).
5. 편집 전 백업 필수: `RUN\backup\<원본명>_20260826_before_<수리이름>.py`
6. **매수·매도 조건, 문턱값, 수량, 슬롯 수는 하나도 바꾸지 말 것.** 이 지시서는 그것을 요구하지 않는다.
7. 완료 보고는 **수정 파일 / 변경 줄 / 테스트 결과** 세 가지만.

---

# 🔴 문제 A (우선순위 1) — 주문이 실제로 나갔는데 엔진은 "실패"로 안다

## A-1. 증상 (오늘 실측, 2건)

```
[2026-08-26 10:33:13] BUY_REJECTED  네이처셀(007390) x0 22950
    REJECTED: SENDORDER_REAL account verification unavailable: no login account

[2026-08-26 11:05:50] SELL_REJECTED 코오롱티슈진(950160) x1 0
    REJECTED: SENDORDER_REAL account verification unavailable: no login account
[2026-08-26 11:06:00] SELL_CONFIRMED 코오롱티슈진(950160) x0 18850 BROKER_ALREADY_FLAT
```

**그런데 같은 시각 브로커 장부(`LOG\broker_journal.log`)에는 체결 기록이 있다:**

```
[2026-08-26 11:05:50][BROKER_v1] event_id=1368534e-0b13-45b5-82f9-1ecdebdf4ca0
    order_no=0348763 state=체결 code=A950160 qty=1 remain=0 otype=-매도
```

즉 **엔진은 "거부"로 기록했는데 실물은 체결됐다.** 이번엔 결과적으로 손익이 맞았지만
(18,620→18,850, +1.235%), 엔진이 실패로 알고 **재시도했다면 이중 매도**가 났을 자리다.

## A-2. 원인 지점 (코드 위치 확정)

`RUN\broker_gateway_v1.py` **3318~3337행**

```python
3318  try:
3319      raw_accounts = str(
3320          self.ocx.dynamicCall("GetLoginInfo(QString)", "ACCNO")
3321      ).strip()
3322      logged_accounts = {
3323          item.strip().replace("-", "")
3324          for item in raw_accounts.split(";") if item.strip()
3325      }
3326  except Exception as exc:
3327      self._write_response(request_id, status="ERROR",
3328          error=f"SENDORDER_REAL account verification unavailable: {exc}")
3331      return
3332  if not logged_accounts:                      # ★ 여기
3333      self._write_response(request_id, status="ERROR",
3335          error="SENDORDER_REAL account verification unavailable: no login account")
3337      return
```

- 키움 OCX `GetLoginInfo("ACCNO")` 가 **일시적으로 빈 문자열**을 돌려주면 즉시 주문을 거부한다.
- **재시도가 없다.** 단 한 번의 빈 응답으로 실주문 경로가 끊긴다.
- 브로커 프로세스 자체는 정상이었다: `state=CONNECTED`, PID 17768,
  08:35부터 **8,880초 무중단**, TR 1,416건 정상 처리 중.

## A-3. 발생 조건 — 상관관계 실측

```
10:15  S06 파이썬 프로세스만 재시작 (브로커는 안 건드림)
10:33  BUY_REJECTED  no login account      ← 재시작 18분 후
11:01  S06 파이썬 프로세스만 재시작
11:05  SELL_REJECTED no login account      ← 재시작  4분 후
```

**09:00~10:15 (재시작 전) 구간에는 0건.** S06 재시작과의 상관이 뚜렷하다.
다만 인과는 미확정 — 브로커를 재기동한 적이 없으므로 "S06 재시작이 브로커 OCX 상태에
영향을 준다"는 경로가 설명되지 않는다. **이 인과 규명이 이 작업의 핵심이다.**

## A-4. 요구 결과

1. **원인 규명**: `GetLoginInfo("ACCNO")` 가 빈 값을 돌려준 진짜 이유.
   - 조사 자료: `LOG\broker_journal.log` 의 11:05:50 전후 60초, `LOG\order_sender_live.log`,
     `IPC\requests\` · `IPC\responses\` 의 해당 request_id.
   - 특히 **11:05:50 에 체결된 order_no=0348763 이 누가 낸 주문인지** 밝힐 것.
     (S06 매도가 거부됐는데 체결이 있었다 — 이전 주문의 지연 통보인지, 실제로 나간 것인지)
2. **재시도 도입**: 빈 응답일 때 즉시 실패시키지 말고 **짧은 재조회**(예: 0.2초 간격 3회)
   후에도 비어 있을 때만 거부한다.
   - ⚠️ **보안 검사 자체를 없애지 말 것.** 3338행의 `account mismatch` 검사(요청 계좌가
     로그인 계좌 목록에 있는지)는 **그대로 유지**한다. 이번 수리는 "빈 응답에 대한
     재시도"만 추가하는 것이지, 계좌 검증을 느슨하게 하는 것이 아니다.
3. **거부와 실물의 불일치 감지**: 주문이 ERROR 로 끝났는데 같은 종목·같은 방향의 체결
   이벤트가 직후에 도착하면 **경고 로그를 남긴다**(주문 재시도 판단은 전략 몫이므로
   여기서는 기록만 한다).

## A-5. 완료 증거 (이 형식으로 보고)

- 수정 파일 / 변경 줄 번호
- 재시도 로직이 동작하는 단위 테스트 1개 (빈 응답 → 재조회 → 성공 / 3회 모두 빈 값 → 거부)
- `tests\test_broker_*` 기존 테스트 실행 결과 (수리 전 개수와 대조할 것)
- 해시 재봉인 결과 (`broker_gateway_v1.py` 는 common 명부에 있음 → 재봉인 필수)

---

# 🟡 문제 B (우선순위 2) — S06 유니버스가 너무 좁아 일을 못 한다

## B-1. 오늘 있었던 변경 (친구님 지시로 Claude 가 이미 적용함)

| 시각 | 변경 | 파일 |
|---|---|---|
| 10:15 | `S06_CHASE_CAP_PCT` 2.0 → **2.5** | `RUN\run_strategy_06_crash_low_chase.cmd` |
| 11:01 | `S06_UNIVERSE_MODE=BOARD` 신설 | 위 .cmd + `RUN\strategy_06_crash_low_chase_v1.py` |

`BOARD` 모드 = 관찰 대상을 **고저폭판 ∪ 돈흐름 선별판의 던짐/매도세** 로 좁힌다.
구현: `_universe()` 안의 분기 + `_mflow_dump_codes()` 헬퍼(1초 캐시).
백업: `RUN\backup\strategy_06_crash_low_chase_v1_20260826_before_board_universe.py`
       `RUN\backup\run_strategy_06_crash_low_chase_20260826_before_board_universe.cmd`

## B-2. 증상

```
관찰 대상   677~829종목  →  57종목 (12분의 1)
11:06 ~ 12:15 (1시간 9분)  CHASE_TRIGGER 0건    ← 그 전에는 10~20분마다 발생
```

**그리고 매수 문턱에 도달했던 3종목이 얼어붙었다** — 유니버스 밖이라 갱신이 멈춤:

```
170920 엘티씨    OBSERVE  반등 +1.51%
104200 NHN벅스  OBSERVE  반등 +2.32%
193250 링크드    OBSERVE  반등 +1.84%
```

## B-3. 왜 이렇게 됐나 (실측 근거)

세 판은 전부 **"오르는 종목·자금이 들어오는 종목"** 을 고르는 판이다:

- 고저폭판 필터: `price_min_krw=10000`, `daily_range_min_pct=10`, `daily_value_min_eok=100`,
  `core_streak_min_days=5` → 어제까지 변동성 크고 거래대금 많은 종목
- 돈흐름 선별판: 기관·외국인 순매수·체결강도 기반. 등급 분포(오늘 12:10) 던짐 20 / 중립 / 매수세 / 매도세
- 통합 후보판의 `attention_rank` 647은 **선별이 아니라 점수 매긴 순위표 전체**

S06 가 찾는 것은 **오늘 −8% 무너진 종목**이라 방향이 반대다.
오늘 급락 37종목 중 **29종목(81%)이 세 판 어디에도 없었다.**

## B-4. 요구 결과 — **선택지를 만들어 오되, 실전 반영은 친구님 승인 전까지 금지**

다음 3안을 **그림자(order-zero)로만** 재고, 하루치 데이터로 비교표를 낼 것:

1. **원복** — `S06_UNIVERSE_MODE` 제거 (스냅샷 전체). 커버 92%.
2. **절충** — 고저폭 ∪ 돈흐름 던짐 ∪ **돈흐름 중립** ∪ 통합판 하위(주목도 낮은 쪽).
   "급락 종목은 주목도가 낮다"는 가설을 검증할 것.
3. **가격 하한 분리** — 유니버스는 넓게 두되, S06 매수 하한 1만원이 진짜 병목인지 재확인.
   오늘 실측: 급락 37종목 중 **1만원 미만이 28종목(76%)**. 하한 미달 종목의 저가대비 반등이
   평균 +7.97%(중앙 +2.50%, 최대 +26.83%)로 통과 종목(+4.83%)보다 높았다.
   ⚠️ 단 이는 **사후 저점 기준**이고 S06 실제 진입가는 저점보다 2~3% 위다. 표본 1일.

## B-5. 완료 증거

- 3안 비교표 (관찰 종목수 / 급락 커버율 / 1만원↑ 커버율 / 가상 진입 성적)
- **실전 반영은 하지 말 것.** 표만 낼 것.

---

# 📌 함께 알아야 할 배경 (조사 중 마주칠 것들)

1. **오늘 S06 성적**: 매수 1건(코오롱티슈진 18,620 → 18,850 +1.235%), 거부 1건(네이처셀).
   `entered_codes` 에 007390 이 등록됨 — **체결도 안 된 거부 주문이 당일 종목 한도를 소진**했다.
   이것도 확인 대상(거부 시 한도를 되돌려야 하는지).
2. **S06 GIVEUP 33건**(오늘). 사유 `ABOVE_CHASE_CAP_IN_OBSERVE` 등 — 저점 +2% 를 넘겨
   추격 포기. 그래서 오늘 2.5% 로 넓혔다. `chase_ceiling`(1291행)은 CHASE·OBSERVE 양쪽이
   같은 `chase_cap_pct` 를 공유한다(하드코딩 0.020 은 존재하지 않음).
3. **기존 테스트 실패는 회귀가 아니다**: `tests\test_strategy_06_crash_low_chase_v1.py`
   9 failed / 17 passed. 원인은 `Config has no attribute 'tp_pct'` 등 **낡은 테스트**이며,
   백업본에도 `tp_pct` 는 없다. 수리 후 이 개수와 대조할 것(새 실패가 숨지 않도록).
4. **S06 재시작 수칙**: `Stop-ScheduledTask` 만으로는 파이썬이 안 죽는다.
   프로세스 종료 후 `Start-ScheduledTask 'STRATEGY06_LIVE'`. 재시작 시 보유 포지션은
   상태파일에서 정상 복구되는 것을 오늘 2회 확인했다.
   ⚠️ **단 재시작 직후 문제 A 가 재발할 수 있으므로, A 를 고치기 전에는 재시작을 최소화한다.**

---

---

# 📊 [13:15 추가 실측] 문제 A 의 실제 피해액 — 오늘 최대 기회를 놓쳤다

## S06 선별은 정상 작동한다. 조건을 건드리지 말 것.

오늘 S06 신호 기록 285행(`data\strategy_06_crash_low_chase\strategy_06_signals_20260826.csv`)
분석 결과, **선별 자체는 문제가 없다.** 깔때기:

```
TRIGGER 37 → OBSERVE_START 37 → PULLBACK_FOUND 18 → DIRECT_READY 5 → BUY_READY 5
동시에 GIVEUP 38 / HIGHER_LOW_RESET 9 / OBSERVE_TIMEOUT 4 / OBSERVE_WAIT 132
통과율 5/37 = 13.5%
```

**BUY_READY 5건의 판정값 (전부 일관)**

| 종목 | 낙폭 | 반등 | 저점 매수/매도비 | flow_flip | 체결강도 |
|---|---|---|---|---|---|
| 차이커뮤니케이션 | −10.6% | 1.95% | 1.38 | O | 59.9 |
| 코오롱티슈진 | −8.7% | 1.69% | 1.73 | O | 48.0 |
| 에이에프더블류 | −12.5% | 1.92% | 2.75 | O | 66.3 |
| 원풍물산 | −8.3% | 1.59% | 4.78 | O | 66.8 |
| 네이처셀 | −8.5% | 1.77% | 2.69 | O | 50.5 |

5건 모두 `dip_buy_sell_ratio > 1`(저점에서 매수 우위) + `flow_flip=O` + 반등 1.59~1.95%.
⇒ **매수 조건·문턱·지표를 조정할 근거가 오늘 데이터에는 없다. 건드리지 말 것.**

## 5건 중 실제 매수는 1건. 나머지 4건은 선별과 무관한 이유로 상실

```
차이커뮤니케이션  2,610원 → PRICE_BELOW_MIN 차단
코오롱티슈진   18,610원 → ✅ 매수. 13:15 현재 18,950 (+1.83%), 실현 +1.235%
에이에프더블류    265원 → PRICE_BELOW_MIN 차단
원풍물산         638원 → PRICE_BELOW_MIN 차단
네이처셀      22,950원 → ❌ no login account 거부
                        13:15 현재 24,350 = +6.10%   ★ 오늘 최대 기회 상실
```

**⇒ 문제 A(계좌 검증 재시도 부재)의 실측 피해가 +6.10% 한 건으로 확인됐다.**
이것이 문제 A 를 최우선으로 두는 이유다. 선별을 아무리 다듬어도 이 관문에서 새면 소용없다.

---

# 🟠 문제 C (추가) — S01 ROCKET 이 구조적으로 뜰 수 없다

## C-1. 증상
어제(8/25) owner 승인으로 오늘이 S01 ROCKET 첫 실전일이었으나 **발동 0건**.
S01 은 오늘 매수 6건 전부 기존 `STRONG_FLOW` 레인이었고 실현 −7.264%p(6전 1승).

## C-2. 원인 (실측 확정)

```
strategy_01_signal_contract_v2.py:23
    ENTRY_V3_MODE = os.environ.get("S01_ENTRY_V3_MODE", "SHADOW")   ← 기본값 SHADOW

RUN\hidden\SAFEPLUS_STRATEGY01_LIVE.cmd / _SIGNAL.cmd
    set S01_ROCKET_LIVE=YES            ✅ 있음
    set S01_TREND_PRIORITY_MODE=SHADOW ✅ 있음
    set S01_ENTRY_V3_MODE=???          ❌ 아예 없음 → SHADOW
```

**오늘 S01 신호 130건 전수** (`data\audit\s01_open_priority\20260826.jsonl`):
```
stage 분포        {'None': 130}          ← v3 stage 가 하나도 안 채워짐
entry_stage 분포  {'STRONG_FLOW': 130}   ← 전부 레거시 레인
```

승격 코드(`strategy_01_open_surge_signal_v2.py:1305~1312`):
```python
eligible = [row for row in entry_v3_rows
            if str(row.get("stage")) == "ROCKET"    ← stage 가 None 이라 영원히 불일치
            and action == BUY_READY and mode == "SIGNAL_ONLY_ORDER_ZERO"]
if not eligible:
    return None      ← 조용히 종료. 탈락 사유 기록 없음
```

⇒ 관문 2개(`S01_ROCKET_LIVE=YES`, `live_features.S01_ROCKET=true`)는 열렸는데
**v3 엔진이 후보를 만들지 않아 승격할 대상이 0.**

## C-3. 요구 결과

1. **`return None` 자리에 탈락 사유 기록을 추가할 것.**
   (예: `entry_v3_rows` 개수, stage 분포, rocket_live 여부 — 신호당 1회, 폭주 방지)
   ⚠️ **이것만 수리 대상이다.** 8/19 S03 사고와 같은 "일 안 해도 아무도 모르는" 구조를 없앤다.
2. **`S01_ENTRY_V3_MODE` 를 켜지 말 것.** 승인 명부에
   *"S01_ENTRY_V3_MODE remains SHADOW until current-path PROD_REPLAY passes"* 로 되어 있어,
   ROCKET LIVE 승인과 **명부 자체가 모순**이다. 이 모순은 친구님이 판단할 사항이며
   GPT 가 임의로 해소하지 않는다. **보고만 할 것.**

## C-4. 완료 증거
- 수정 파일 / 변경 줄
- 탈락 사유가 실제로 기록되는지 단위 테스트 1개
- `tests\test_strategy_01_*` 실행 결과 (수리 전 개수와 대조)
- 해시 재봉인 결과 (`strategy_01_open_surge_signal_v2.py` 는 S01 명부에 있음)

---

## 착수 순서 (권고)

```
1. (지금~15:30) 읽기 전용 조사만:
     - 문제 A: order_no=0348763 추적, broker_journal 11:05:50 전후 60초
     - 문제 B: 3안 비교표
     - 문제 C: v3 엔진이 왜 stage 를 안 채우는지 경로 확인 (읽기만)
2. (15:30 이후) 수리 착수 — 우선순위 A → C → B
     각 건마다: 백업 → 편집 → 테스트(기존 실패 개수 대조) → 해시 재봉인
3. 문제 B 는 표만 제출. 실전 반영은 친구님 승인 후.
4. 문제 C 의 S01_ENTRY_V3_MODE 활성화는 절대 하지 말 것. 보고만.
```

---

## 🚫 GPT 가 하면 안 되는 것 (재확인)

- 매수·매도 **조건/문턱/수량/슬롯** 변경 — 오늘 데이터에 근거 없음
- `S01_ENTRY_V3_MODE` 활성화
- 문제 B 의 유니버스 실전 반영
- 계좌 대조 검사(`broker_gateway_v1.py:3338`) 완화 — 재시도만 추가하는 것이지 검증을 푸는 게 아니다
- S02·S03·S05·돈맥·공용매도 파일 열기
