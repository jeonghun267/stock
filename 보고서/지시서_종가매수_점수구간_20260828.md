# 지시서 — 종가매수(EOD_GAP) 점수 문턱을 "70 이상"에서 "60~70 구간"으로 변경

작성 2026-08-28 저녁 · 승인자 친구님("지피티한테 지시문을 내려") · 작성자 Claude(검증자 겸임)
집행자: GPT · 검증자: Claude (역할 분리 — CLAUDE.local.md 4절)

## 0. 왜 (근거 — 2026-08-28 실측, 로그 49일·222건)

역대 후보를 "당일 종가 매수 → 익일 시가 매도"로 집계한 결과:

| 점수 구간 | 건수 | 익일시가 평균 | 승률 | 익일종가 평균 |
|---|---|---|---|---|
| 70 이상(현행 매수 대상) | 49 | +1.50% | 49% | **-4.03%** |
| **60~70(현행 컷 구간)** | 57 | **+3.16%** | **68%** | +1.37% |
| 50~60 | 43 | +0.80% | 47% | -1.09% |

현행 문턱은 최적 구간을 자르고 과열 구간만 산다. 검증 스크립트:
`scratchpad\eodgap_score_analysis.py` (Claude 세션, 재현 가능).

## 1. 원하는 결과

1. `RUN\eod_gap_live_executor_v1.py`: 점수 **상한** 지원 추가 —
   `EOD_GAP_MAX_SCORE` env(기본 없음=무제한, 하위호환). 매수 자격 =
   `MIN_SCORE <= score` **AND** (`MAX_SCORE` 설정 시 `score < MAX_SCORE`).
2. `RUN\hidden\SAFEPLUS_EOD_GAP_PICK.cmd`: `set EOD_GAP_MIN_SCORE=60` ·
   `set EOD_GAP_MAX_SCORE=70` 추가.
3. 로그에 구간 컷 사유를 남길 것: 상한 초과 탈락 시
   `SCORE_ABOVE_MAX(점수/상한)` 한 줄 (내일 판독용).

## 2. 반드시 지킬 제약

- **이 두 파일 외 수정 금지.** 특히: 전일감사 재생 게이트(LIVE 강등 로직)·
  LOCKED/LOWBUY 경로·매도(sell) 모드·타 전략 일절 무접촉.
- 수정 전 백업: `RUN\backup\*_20260828_before_score_band.*` 2개.
- `.cmd`는 CRLF·ASCII 유지 (오늘 LF 혼입 사고 반복 금지 — 저장 후 바이트 확인).
- 명부: 두 파일 모두 승인 명부(63건)·관리자 기준표(16건) **커버 밖임을 실측
  확인 후** 진행(커버였다면 중단·보고). 확인 명령:
  `Select-String config\live_approved_hashes_v1.json, config\elevated_path_hashes.json -Pattern "eod_gap"`
- 실행 중 프로세스 재시작 금지. 효력은 다음 거래일 15:18부터.
- 라벨: 실전 효과는 다음 거래일 실측 전까지 **[UNVERIFIED]**.

## 3. 완료 증거 (보고에 반드시 포함)

1. diff 요약 (두 파일 각각, 변경 줄 수)
2. 문법: `py_compile` OK + 관련 집중 테스트 결과 (있으면 실행, 없으면 "테스트 없음" 명기)
3. cmd 바이트 검사: bareLF=0, nonASCII=0
4. 명부 커버 밖 확인 결과 (위 명령 출력)
5. 시뮬레이션 1건: 오늘(8/28) 후보 목록에 새 구간을 적용하면 무엇이 살아나는지
   (예상: 우리기술투자 69.1점 → 자격 획득, 87점류 → SCORE_ABOVE_MAX 탈락)

## 4. 참고 (건드리지 말 것, 별건)

- 전일감사 부트스트랩 자물쇠는 8/28 처음 감사가 저장되어 **다음 거래일 15:18에
  자동 시험**된다 — 이 지시서 범위 아님.
- LOCKED 경로 재점화·LOWBUY 태스크(7/31부터 Disabled)는 친구님 별도 결정 대기.
