# GPT 작업 지시문 (2차·확정판) — S06 마무리 + S02 잔여 정리 (2026-08-20 저녁)

> 1차 지시문(GPT지시문_S02수정_20260820.md)은 폐기. 이 문서가 대체한다.
> 아래는 전부 실측으로 확인된 문제만이다. 여기 없는 것은 고치지 말 것.
> 발견한 다른 문제는 수정 금지 — 한 줄 보고만.

---

## A. 필수 수리 3건 — 오늘 밤 완료 (안 하면 내일 S06 수정이 무효가 되거나 기동이 막힘)

### A-1. S06 런처가 옛 값으로 코드 수정을 덮어쓴다 → 런처 값 갱신

`RUN\run_strategy_06_crash_low_chase.cmd` 의 아래 4줄을 새 값으로 바꿀 것.
(코드는 env 우선이라, 이 줄들이 남아 있으면 오늘 고친 코드 기본값이 전부 무시된다 — 실측 확인됨.)

```
set S06_REBOUND_PCT=1.5      →  0.5
set S06_ENTRY_FLOOR_PCT=1.0  →  0.5
set S06_CHASE_CAP_PCT=2.0    →  1.5
set S06_TP_PCT=10.0          →  5.0
```

- `RUN\hidden\SAFEPLUS_STRATEGY06_RECOVERY.cmd` 에도 같은 set 줄이 있는지 확인하고, 있으면 동일하게 맞출 것.
- .cmd 는 CRLF + ASCII 전용. 편집 후 CR/LF 개수·비ASCII 0 확인값을 보고에 포함.

**완료 증거**: 두 런처의 diff + CRLF/ASCII 확인값.

### A-2. 승인 명부 해시 불일치 → CAS 갱신

S06 엔진(`strategy_06_crash_low_chase_v1.py`)의 현재 해시(983c575a…)가 명부와 불일치 — 이대로면
내일 08:55 승인 게이트 FAIL 가능. 반드시 `RUN\approval_manifest_writer_v1.py`(CAS)로만 갱신할 것.

- 공용 `low_rebound_common_v1.py`(오늘 8줄 수정: epsilon + 주석)가 명부에 등재돼 있으면 함께 갱신.
- A-1 런처 수정 후의 해시로 갱신할 것 (순서: A-1 → A-2).
- scope 사유에 "2026-08-20 owner 직접 지시 S06 파라미터 완화(반등0.5·바닥0.5·포기1.5·익절5)" 명기.

**완료 증거**: 갱신 후 명부의 S06 항목 해시 == 현재 파일 해시 (대조 출력).

### A-3. 백업 스냅샷 생성 (규약 이행)

`RUN\backup\strategy_06_crash_low_chase_v1_20260820_after_param_ease.py` 로 현재본 저장.
(편집 전 원본은 git HEAD 에 있음 — 원복 명령 한 줄도 보고에 적을 것.)

## B. 확인 후 답변만 2건 — 수정하지 말고 판단 근거를 보고 (친구님 결정 사항)

### B-1. 숫자 기하학: ①첫 반등 +0.5% 와 ③눌림 저점 ≥ 원저점+0.3% 의 긴장

첫 반등이 +0.5~0.7% 사이에서 꺾이면 0.4% 눌림 시 저점이 +0.3% 아래로 내려가 ③에서 탈락
→ 실질 첫 반등 문턱은 약 +0.7%. "0.5로 낮춰 빨리 잡는다"는 의도와 어긋나는지 판단 근거
(최근 5일 S06 트리거 종목의 첫 반등 분포)를 산출해 보고할 것. ③의 0.3 조정 여부는 친구님 결정.

### B-2. `S06_EARLY_ENTRY_CAP_PCT=1.8` (런처 잔존) vs 새 포기선 1.5

조기진입 상한(1.8)이 포기선(1.5)보다 높다. 코드에서 이 변수의 실제 사용처와 우선순위를 확인하고,
모순이면 어느 쪽을 기준으로 정렬할지 선택지만 보고 (수정 금지).

## C. S02 잔여 정리 2건 (재검증 후 살아남은 것만 — 적응바닥은 재생 PASS 확인됐으므로 손대지 말 것)

### C-1. 낡은 주석 1줄 정리

`strategy_02_low_buy_signal_v1.py` 295행 부근 "[UNVERIFIED] 재생 4개 중 1개만 재현 → 기본 OFF"
주석은 이후 재생 PASS(prod_replay_s02_exact_inputs_20260820.json, 17,918행)로 낡았다.
PASS 사실·증거 경로를 반영해 주석만 갱신 (로직 무변경).

### C-2. 지수 산출 오후 중단 원인 규명 (수리 아님, 조사 보고)

`data\BACKTEST\regime_std_shadow.csv` 의 band 가 2026-08-20 14:47부터 마감까지 연속 NO_INDEX
(u201_chg 빈 값). `kosdaq_index.json` 은 5분마다 갱신 중이었다. 어디서 끊기는지 원인만 보고.
S02 매수창(~14:20) 밖이라 오늘 실해는 없었으나, 아침에 재발하면 적응바닥이 UNKNOWN 차단된다.

---

## 반드시 지킬 제약 (공통)

1. S06 엔진 로직(파이썬 판정 코드)은 이 문서에 없는 한 수정 금지 — 친구님 직접 수정 영역.
2. 장중(08:40~15:30) 실전 파일 편집 금지 — 이 작업은 오늘 밤 완료.
3. 명부는 CAS 도구로만. `.cmd` CRLF·ASCII. PowerShell로 JSON 쓰기 금지.
4. pytest 기준선 38 failed — 신규 실패 0 확인.
5. 매도 규칙·트레일 접근 금지.
6. 완료 보고 형식: 항목별 "diff 요약 · 실행 증거 · 롤백 한 줄" 3종 세트.
