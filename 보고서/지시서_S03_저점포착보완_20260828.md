# 지시서 — S03 저점 포착 보완 (신저점 이탈 그림자 + 레인 표기 + ATR/지수 그림자)

작성 2026-08-28 밤 · 승인자 친구님("지시서 내려") · 작성자 Claude(검증자 겸임) · 집행자 GPT
⏰ 집행: **8/31(월) 15:30 이후** (장중 편집 금지 · S01 작업 B와 같은 저녁에 일괄 —
S03 파일 재봉인을 1회로 묶는다). 한 번 수정 후 화요일 장중엔 동결.

## 0. 왜 (8/28 실측 — 매수 33건 전수)

- 저점 대비 평균 **+5.01%** 위에서 매수(저점 ±2% 포착 18%) · 승률 27% —
  추적 저점이 진짜 저점이 아니었다(매수 후 신저점 갱신 = 칼날 매수).
- 신저점이 갱신돼도 고정 -2% 하드스톱만 기다린다(진입 근거 붕괴 손절 부재).
- 매수 이벤트에 레인 표기가 없어(`exact fill rank`뿐) 계단/급락 성과 분리 불가.
- 문턱 전부 고정 %(변동성 무시)·지수 급락 가속 중에도 진입.

## 1. 작업 (전부 한 번의 수정으로 — S03 재봉인 1회)

### A. [그림자] 신저점 이탈 손절 — 기록만, 실매도 불변
1. 매수 시 position 에 **진입 근거 저점**(신호가 추적한 저점)을 저장한다
   (S01 `pullback_reference_low` 와 같은 방식 — `s03_entry_reference_low`).
2. 보유 중 가격이 그 저점 -0.3% 아래 + 매도 재가속(S01 `_pullback_failure_reason`
   조건 재사용)이면 `would_exit_low_break=true` 를 매도 감사에 기록.
   **실매도 판정은 1도 바꾸지 않는다.** 5거래일 후 재생으로 실전화 판정.

### B. [즉시 유효] 매수 이벤트 레인 표기
신호의 레인(OPEN_CRASH/INTRADAY_CRASH)을 position 과 BUY_CONFIRMED reason 에
붙인다 — 감사 개선(판정 무변경). 예: `exact fill rank=1/1 lane=OPEN_CRASH`.

### C. [그림자] ATR 정규화 + 지수 필터 — 기록만
매수 확정 시점에 함께 기록: ①그 종목 ATRP10(확정 일봉 — S06 의
`atrp10_pct()` 재사용) 과 "낙폭/ATRP 배수" ②코스닥 지수의 직전 5분 변화
(kosdaq_index.json). 문턱 판정은 하지 않는다 — 5거래일 모아
"ATR 정규화·지수 필터를 걸었다면"을 데이터로 판정한다.

## 2. 반드시 지킬 제약

- 수정 파일: S03 신호기·엔진·계약 중 필요한 최소 + 테스트. **매수 문턱·반등띠·
  매도 실행·타 전략 무접촉.** 전부 기록 추가 성격(B의 reason 문자열 제외 판정 무변경).
- 백업 필수(`*_20260831_before_s03_low_shadow.*`) · **명부 재봉인 한 세트**(S03 등록
  파일들 — 어기면 화요일 관문 FAIL. 이번 주 4회 반복된 함정).
- .cmd 접촉 없음 예상 — 만지게 되면 CRLF·ASCII 유지.
- 재시작 금지(화요일 08:55 자동 기동이 로드). [UNVERIFIED] — 화요일 실동작이 첫 검증.

## 3. 완료 증거

1. diff 요약(파일·줄 범위) + 백업 목록
2. 컴파일 + 집중 테스트(기존 + 신규 3: A 그림자 기록·B 레인 표기·C 기록 필드)
3. 재봉인 출력 + 관문 4종 PASS
4. 화요일 감사 샘플에 `would_exit_low_break`·`lane`·`atrp` 필드가 실제로 찍히는지
   확인 방법 1줄

## 4. 판정 기준 (5거래일 후, 친구님 결정 재료)

- A: "신저점 이탈 즉시 팔았다면" 33건형 칼날 손실이 얼마나 줄었는가
- C: ATR 정규화·지수 필터 would-block 건의 실제 성적 — 걸 가치가 있는가
- 이후 실전화는 각각 별도 지시서·재생 검증으로.
