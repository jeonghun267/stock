# 장 마감 후(15:30~) 작업 목록 — 2026-07-31(금)

> 작성: 7/31 08:20 · 장 시작 전 읽기 전용 점검 결과
> **장중에는 아무것도 실행하지 말 것.** 전부 15:30 이후.
> 각 항목은 친구님 승인 후 하나씩 진행. 동시 진행 금지(원인 추적 불가).

---

## ★★ 친구님 최종 결정 (7/31 14:2x) — 저점 사냥 통합 체계 ★★

> *"결정했다. 여기서만 하자(고저폭 TOP30). 여기 꺼 추적하면서 사는 걸로 하고 —
> 급상승 -1~-3% · 보통 -3~-9% · 종가매수 -9%를 잡는다."*

### 확정 명세
```
공용판 = 고저폭 TOP30 (전용 구독 50칸 · 그림자 2초 추적 — 이 판만 보고 산다)

아침 09:00 장세판정 (예상체결 상승비율: 오늘 93%→급상승 실증)
├─ 급상승일 : 시가 대비 저점  -1 ~ -3%  에서 반등확인 매수   ← 오늘 30/30 전원 플러스 실증
├─ 보통일   : 시가 대비 저점  -3 ~ -9%  에서 반등확인 매수
└─ 공통     : 저점 깊이 -9% 이하 도달 종목 → 15:18 종가매수 · 익일 이월
```
- 방아쇠: 저점 +1% 반등 확인(검증된 LOWBUY 방식) · 1주 · 중복매수는 밴드배정+매수예약 장부로 차단
- 기존 상한가 종가매수(EOD_GAP)는 별도 유지(검증된 엣지 — 이 체계와 무관)

### 내가 채운 기본값 (친구님이 안 정한 부분 — 바꾸실 수 있음)
1. **매도**: 장중 밴드 = 당일 청산(급상승일은 장 끝까지, 보통일은 매수 후 1시간 — 34일 백테 근거)
   · -9%↓ 종가매수 = 익일 종가(백테 최강) + 기존 LOWBUY 매도기 안전선(이평 상승보유·하드-3%)
2. **급하락일**: 장중 밴드 정지(백테 293건 적자) — -9%↓ 종가매수만 가동
3. **급상승일 이월 금지**: 급상승일 매수는 당일 청산(이월은 미검증)

### 근거 실측 (오늘 하루)
- 급상승일 저점매수: 34일 백테 +1.92~+3.48% (6일) + **오늘 실측 30/30 플러스·중앙 +7.41%**
- 보통일: -3~-6 적자·-6~-10만 +2.28%(1h) → -3~-9 통합 밴드는 그림자로 세부 확인
- -9%↓ 종가매수: 이 판(급등계열)에서는 미검증 ⚠️ — 전체 유니버스 백테는 -10~-15 얇음(-15~-25만 강함).
  **그림자로 반드시 확인 후 실전** (친구님 원칙: 새 기능은 그림자/소액 우선)

### 배선 계획 (승인 대기)
1. 오늘 밤: 그림자 배선(주문 0) — 기존 저점매수 그림자에 3밴드 가상체결 + 아침 장세판정 기록
2. 월요일: 그림자 실측 가동 → 저녁 결과 보고
3. 이상 없으면: 실전 전환(1주씩) — LOWBUY 진입기의 문턱·창을 이 명세로 교체

---

## 0-A. 속도·가격차 실측 (친구님 요청 7/31 08:30)

> 배경: *"매도할 때 차트 가격하고 창에서 매도하는 가격이 다르게 나오는 것 같다"*
> 원인 후보 3개를 분리해서 각각 실측한다.

### A. 화면 지연 — 09:05 측정
`IPC\live_micro_snapshot.json` 의 종목별 `ts` vs 현재 시각 = **지금 화면 값이 몇 초 전 것인가**
```
지표: 나이(초) 의 중앙값·90분위·최대. 15초 넘는 종목 수.
의미: 15초 넘으면 진입기가 그 종목을 아예 버린다(SHADOW_MAX_AGE_SECONDS).
```

### B. 주문 지연 — 첫 실주문(11:00~) 이후 측정
어젯밤 배선 `[ORDER-EVT 2026-07-30]`. **현재 데이터 0건** — 배선 후 실주문이 없었음.
```
자료: data\LOG\event_journal_20260731.jsonl
  ORDER_SUBMITTED.age_sec  = "팔자/사자" 결정 → 게이트웨이 접수까지 (IPC 지연)
  ORDER_RESULT.send_ms     = 키움 SendOrder 자체 소요
주의: age_sec > 8 이면 게이트웨이가 주문을 거부한다(BROKER_ORDER_MAX_TTL_SEC=8).
```

### C. 실제 슬리피지 — 15:31 측정
**관측가 vs 실제 체결가.** 이게 진짜 답이다.
```
관측가: DATA\eod_gap_positions.json 의 buy_price (= 진입기가 본 현재가)
체결가: opw00018 의 매입단가 (15:31 fillcheck 가 어차피 잔고를 조회함)
차이 = 실제 슬리피지. 1만원 종목 호가 1칸 = 10원 = 0.10% 가 기준선.
```

**이미 확정된 것 (재측정 불필요)**
- 우리 주문은 전부 `price=0, hoga_gb="06"` = **최유리지정가**
- 최유리 매도는 **매수 1호가**에 걸리고, 차트 현재가는 **마지막 체결가** → 원래 다름
- 호가 1칸: 1만원대 10원(0.10%) · 3만원대 50원(0.17%) · 6만원대 100원(0.17%)
- 기존 실측: 왕복비용 0.29~0.47% · 손절 슬리피지 0.09%p

---

## 0-B. ✅ S03 수술 완료 (7/31 낮 · 수정 2건 · 월요일 08:55부터 적용)

### 최종 진단 (당초 "70% 평가불가"는 과장 — 유령행 105개를 분모에 넣은 오류. 실제는 명단 116 중 43~50% RESET)

**원인 사슬 (전부 실측으로 확정)**
```
호가(ob_ts) 갱신이 일부 종목에서 띄엄띄엄   ← 관문 탈락 5,641회 (체결 4,419회보다 큼)
  ↓ 4초 이중관문(체결ts+호가ob_ts 둘 다)을 자주 탈락
엔진이 그 종목 데이터를 띄엄띄엄 수용 (엔진 바퀴 6.7초 실측)
  ↓ 수용 간격이 60초 초과
이력 리셋 반복 → 10분 창을 영영 못 채움 (RESET 종목 재평가 간격 중앙 91초)
```
**배제된 가설(실측)**: 누계역전 0건 · 60초 데이터공백 1.9%뿐 · 저유동성 1.3배(무의미) ·
찢김방지 1.7%뿐 · 체결 공급은 정상(명단 종목 신선 92~98%)

**"7/29부터 갑자기 안 됨"의 답**: 코드가 그날 망가진 게 아니다. 급락 순간에만 체결이
1~3초로 촘촘해져 창이 쌓이는 구조라, 7/27·28엔 명단 안 종목이 장중(레인 열린 시간)에
급락해 잡혔고, 이후엔 급락이 09:00~09:11(레인 닫힘)이거나 명단 밖이었다.
S02 동시 사망은 별건 — 5분 관측창이 구조적으로 못 잡던 것(7/30에 30분으로 수정 완료).

### 수정 2건 (친구님 승인 · 회귀 14/14 · py_compile OK)
| # | 파일 | 변경 | 백업 |
|---|---|---|---|
| ① | `strategy_03_intraday_rebound_v1.py:73` | `max_gap_sec` 60→**150초** (재평가 간격 91초+여유) | `*_20260731_gap150.py` |
| ② | `골짜기_급반등.py:51` + `signal_contract_v1.py:62` | 장중 레인 **09:20→09:00** (쌍으로) | `*_20260731_lane0900.py` ×2 |

②검증: 실제 7/28 통과 신호로 09:05 통과 · 08:59 거부 · 14:25(원본) 통과 확인.
문턱(-3%)·반등(0.5~1.0)·매수비율(0.58)·OPEN_CRASH 레인은 무변경.

### 적용·확인
- 7/31 낮 재기동 시도 → 옛 프로세스가 관리자 권한이라 안 죽음(schtasks /end 무효) → **월 08:55 자동 적용**
- **월요일 장중 확인**: 신호판 `ENTRY_TIME_CLOSED`·`CUMULATIVE_..._RESET` 비율 급감 여부 (기준: 7/31 RESET 50%)
- 잔여 별건: **호가 구독 커버리지**(화면당 100종목 한계) — 브로커 쪽. 재기동 필요라 별도 승인 사안.

<details><summary>당초 조사 기록 (보존)</summary>

## (구) S03가 후보의 70%를 평가조차 못 하고 있다 (7/31 09:32 실측 발견)

> 발단: 친구님 "급락주 잡는 전략 3이 왜 안 잡나". 처음엔 "오늘 급락주가 없다"고 봤으나
> **1분봉으로 직접 재보니 아니었다.** 조건은 충족했는데 다른 이유로 막혀 있었다.

### 실측 (09:31 · `data\돈맥_1분봉.json` 으로 독립 계산)

최근 10분 창 기준 낙폭이 -3% 이하인 종목이 실제로 있었고, **전부 낙폭이 아닌 딴 이유로 탈락**:

| 실측 낙폭 | 종목 | S03 판정 |
|---|---|---|
| -7.94% | 엠케이전자 | `CUMULATIVE_REVERSE_OR_DATA_GAP_RESET` |
| -5.37% | RF머트리얼즈 | `CUMULATIVE_REVERSE_OR_DATA_GAP_RESET` |
| -4.78% | 아스플로 | `ENTRY_TIME_CLOSED` |
| -3.51% | 데브시스터즈 | `CUMULATIVE_REVERSE_OR_DATA_GAP_RESET` |
| -3.36% | 마키나락스 | `ENTRY_TIME_CLOSED` |

### 원인 2가지

**① 시세 끊김 — 61종목 (INTRADAY_CRASH 레인 177건 중)**
```
ENTRY_TIME_CLOSED 행의 ts:  최신 09:18:44 · 가장 오래 08:59:08 · 평균 나이 1,300초
```
레인은 09:20에 열렸는데 **이 61종목은 09:18 이후 시세가 안 들어와 판정이 얼어붙었다.**
레인 개폐와 무관 — **시세가 없으니 평가 자체가 안 된다.**

⚠️ 기존 미조치 항목과 증상이 일치:
*"전략 감시 종목이 실시간 구독을 보장받지 못한다 — 돈맥 급락종목 195개가 상한 200을
독점해 strategy_shared(138)·strategy(100)이 0칸"* → **구독 칸 고갈 의심. 확인 필요.**

**② 이력 계속 리셋 — 63종목**
`strategy_03_intraday_rebound_v1.py:_append()`
```python
reversed_counter = 누적 매수/매도 수량·금액이 이전보다 작아짐
if reversed_counter or gap > max_gap_sec:
    self._reset_history()      # ← 10분 이력 통째로 버림
```
발동하면 10분 창이 리셋된다. 계속 발동하면 **창이 영영 안 쌓여 낙폭 계산이 원천 불가.**
오늘 제일 깊게 빠진 종목들이 전부 이 상태였다.

### 결론
```
S03 후보 177종목
├─ 61  시세 끊김        → 평가 불가
├─ 63  이력 계속 리셋    → 낙폭 누적 불가
└─ 53  정상 평가 (30%)
```
**정상 심사받는 게 30%뿐이다.** S03가 "신호 0"인 건 시장에 급락주가 없어서가 아니다.

### 다음에 할 것 (수정 아님 — 원인 규명부터)
1. `reversed_counter` 와 `gap > max_gap_sec` 중 **어느 쪽이 리셋을 일으키는지** 분리 계측
   (누적 카운터 역행이면 데이터 소스 문제, gap이면 구독 문제)
2. `max_gap_sec` 현재값 확인 — 너무 짧으면 정상 시세도 리셋으로 오판
3. 61종목이 **실제로 구독 목록에 있는지** 브로커 구독 현황과 대조
4. 위 3개 결과 나온 뒤에야 고칠 것을 정한다. **지금 추측으로 손대지 말 것.**

### 부수 발견 (보고 화면 결함)
`INTRADAY_DRAWDOWN_LT_3PCT` 판정을 내리면서도 `intraday_drawdown_pct` 는 **0.0으로 찍힌다**
(무장 후에만 채워지는 필드를 보고용으로 씀). **이 값만 보면 낙폭을 알 수 없다** —
오늘 내가 처음에 "급락주 0건"으로 오판한 원인. 진단 시 반드시 원자료로 재계산할 것.

</details>

---

## 0-G. ✅ 종가매수 티어 백테 완료 + 장세 구분 실증 (7/31 13:30 · 저녁 확정 안건)

### 백테 (일봉 1년 · 종가매수 → 매도규칙 비교 · 비용 0.4%p 차감)
- **기준선은 전일종가 대비가 정답** (시가 대비 저점은 전 구간 적자)
- **구간은 -15~-25%** (-10~-15%는 +0.58%로 얇음 · -25%↓는 -3.19% 적자 재확인)
- **매도는 익일 종가가 최강**: +5.18%/65% (-15~-20%) · +10%터치는 +3.02%로 2%p 열세(머리 자름)
- **대금은 작을수록 좋다**: 30~100억 +7.23%/74% > 700억↑ +2.70% → 이 티어는 하한 30억
  (기존 "700억~2조"는 장중 지정가추적 전략 결론 — 모순 아님)

### 장세 구분 — 친구님 직감 2중 실증
**① 종가매수 티어는 하락장 전용이다** (승자규칙 -15~-25%·30억↑·익일종가):
```
BEAR    n=402  비용후 +5.87% · 승률 65%   ← 엣지 전부 여기
NEUTRAL n= 63  비용후 -0.60% · 승률 44%   ← 적자! 게이트 필요
BULL    n=  2  (후보 자체가 없음 — 자연 휴무)
```
→ **레짐 게이트: BEAR 에서만 가동.** NEUTRAL 정지.

**② 오늘(급등장) 30종목 실측** — 상승장엔 기준선별 인구가 완전히 다르다:
```
시가比저점   : 26/30 이 0~-3% · -10%↓ 0개     → 낙폭 밴드 사실상 빈집
전일比저점   : 중앙 +7.35% · -15%↓ 0개        → 종가매수 후보 0 (자연 휴무 확인)
고점比현재   : -3%↓ 12개 · -6%↓ 3개          → 눌림 기준만 인구 있음
```
→ 상승장에 뭘 사려면 **장중고점 눌림 기준**뿐인데, 그건 눌림목(S04) 영역과 인접 —
  단독 결정 말고 그림자 검증 먼저.

### 확정 후보 (저녁 승인 대기)
```
종가매수 티어: 전일比 -15~-25% · 대금 30억↑ · 가격 1만↑ · BEAR 게이트 · 15:18 종가매수
              → 익일 종가 매도 (그림자로 '이평 상승보유' 대안 병행 기록)
```
한계: 종가·터치 체결 가정 · 표본 1년 · regime 컬럼은 당일 확정값(15:18 시점 인지 가능).

---

## 0-E. 💡 친구님 아이디어 — 깊이로 S02/S03 역할 분담 (7/31 10:07 · **저녁에 논의**)

> 친구님: *"전략 2는 아침 저점을 **-9% 이하만** 잡는 걸로 만들고, 전략 3은 **-3% 이하**로
> 같이 만들면 어떨까. 이따가 생각해보게."*

### 구상
```
S02  아침 깊은 저점   -9% 이하   ← 드물지만 큰 반등
S03  장중 얕은 급락   -3% 이하   ← 자주 나옴
```
**깊이로 사냥터를 나누는 것.** 지금은 둘이 서로 다른 방식(파동 비교 vs 낙폭)이라
역할 경계가 흐리다. 깊이로 자르면 명확해진다.

### 오늘 실측 — 각 규칙이 몇 개를 잡나 (7/31 09:56, 시가 대비 기준)
```
-13% 이하 :  2종목   (동운아나텍 -17.79% · 디엔에프 -13.64%)
 -9% 이하 :  3종목   (+ 코스텍시스 -9.76%)
 -8% 이하 :  5종목
 -5% 이하 : 11종목
 -3% 이하 : 더 많음
```
→ **-9%는 하루 3종목 수준.** 빈도는 낮지만 반등폭은 컸다(+4.4% · +9.9% · +10.8%).

### 저녁에 반드시 짚을 것 3가지

**① S03는 이미 -3% 이하다.** `intraday_drawdown_pct <= -3.0` (`signal_contract_v1.py:61`).
   바꿀 게 없다. **다른 건 기준선**: S03는 **10분 창 고점** 대비, 친구님 구상은 **시가** 대비.
   → 진짜 논점은 문턱이 아니라 **"기준선을 시가로 통일할 것인가"**.

**② S02에는 낙폭 문턱이 아예 없다.** 지금은 `detect_flow_book_exhaustion` =
   **하락 파동 2개를 비교**하는 방식. -9%를 넣는 건 파라미터 조정이 아니라 **전략 재설계**다.
   기존 매도소진 로직을 버릴 것인지 병행할 것인지 결정 필요.

**③ 유니버스 제약이 먼저다.** -9%/-13% 종목은 오늘 **전부 거래대금 100억 미만**이라
   고저폭 30 밖이었다(0-D 참조). **문턱을 정해도 유니버스가 안 보면 소용없다.**
   순서: 유니버스 → 기준선 → 문턱.

### 결정 전 확인
3일 그림자(7/31·8/3·8/4)로 **-9% / -5% / -3% 각 문턱의 빈도와 비용 반영 후 수익**을 비교.
빈도가 하루 3종목이면 표본이 너무 적어 3일로도 부족할 수 있다 — 기간 연장 검토.

### 🔶 범위 확대 — 친구님 지시 (7/31 10:10)
> *"저녁에 그 기준선 통일하는 거 같이 보자. 기준선도 보완을 하고 **전략을 다시 짜야 될 것 같다.
> 전략 2, 3번 두 개 다.**"*

**문턱 조정이 아니라 S02·S03 재설계 안건이다.** 저녁에 함께 검토.

**저녁까지 내가 준비해 둘 것**
1. **현행 대조표** — S02/S03 의 기준선·문턱·진입창·판정방식을 한 장에
2. **기준선 후보 비교** — 시가 / 전일종가 / 장중고점 / 10분창고점 각각
   오늘 하루 몇 건 잡히고 어떤 종목인지 (전일종가는 이미 0건으로 기각 — 0-C 참조)
3. **재설계 시 제약 목록** — 유니버스(대금 문턱)·구독 통로 상한 50칸·왕복비용 0.29~0.47%
4. **오늘 실전 결과** — S01~S05 + 저점매수 실측

**재설계 시 지킬 원칙 (친구님 CLAUDE.md)**
- 한 번에 하나씩 바꾸고 검증한다 (I항: 1회 반복에 수정 1개). 둘을 같이 바꾸면 원인 추적 불가.
- 기존 검증을 인상으로 뒤집지 않는다. 뒤집으려면 그림자 실측이 먼저.
- 새 패턴 도입 금지 — 기존 코드 패턴을 복사하는 것이 기본값.

---

## 0-F. 💡 친구님 설계안 — 저점을 깊이로 3단 분류 (7/31 11:09 · **저녁 본안건**)

> *"저점 잡는 방법을 3가지로 분류하면 어떨까. 전략2 · 전략3 · 종가매수 셋으로 나눠서
> **전략2 -3% 이하 / 전략3 -6% 이하 / 종가매수 -10% 이하**. 종가매수는 **당일 10% 고점이 찍히면
> 매도**하고 안 찍히면 다음날로 끌고 가고, 전략2·3은 현행대로 매도. **시간창은 9시부터.**
> 오늘은 상승장이라 저가매수 잡기 힘들 것 같고, 코스닥이 최근 많이 떨어졌기 때문에 오름세
> 가능성이 커. 오름세로 돌아서면 놓칠 게 많을 것 같다."*

### 구상 요약
| 전략 | 깊이 문턱 | 매도 | 시간창 |
|---|---|---|---|
| S02 | **-3% 이하** | 현행 유지 | 09:00~ |
| S03 | **-6% 이하** | 현행 유지 | 09:00~ |
| 종가매수(LOWBUY) | **-10% 이하** | **당일 고점 +10% 도달 시 매도 / 미도달 시 익일 이월** | 09:00~ |

**장점**: 지금은 세 전략의 사냥터 경계가 흐리다(기준선·판정방식이 제각각). 깊이로 자르면
역할이 명확해지고 "어느 전략이 벌었나"를 비교할 수 있다.

### 시장 근거 확인 (7/31 11:08 실측)
```
코스닥 지수  698.06 (전일 644.78) → 오늘 +8.26% 급등
레짐 판정    market_regime_20260731 : BEAR  (7/30 종가 기준·ma20 아래·기울기 -2.2·breadth 11.0)
             market_regime_std      : BULL  (실시간·breadth 0.85·나스닥 +2.78)  ※SHADOW 상태
```
→ **어제까지 BEAR, 오늘 BULL 전환 중.** 친구님 판단이 데이터로 뒷받침된다.
   (다만 지수는 3000이 아니라 **698**. 숫자만 정정)

### 저녁에 반드시 짚을 것 4가지

**① 기준선이 정해지지 않으면 -3/-6/-10 숫자는 의미가 없다 — 최우선 논점**
오늘 11:03 실측으로 같은 종목이 기준선에 따라 완전히 달라진다:
```
두산테스나   장중고점 +25.09% → 현재 +13.57%
             시가 대비    +13.57%  → 어떤 문턱에도 안 걸림
             장중고점 대비 -9.21%  → -6% 문턱 통과
```
- **시가 기준**: 오늘 -10% 이하 = 1종목뿐(상승장이라 안 빠짐) → 세 전략 다 논다
- **장중고점 기준**: 급등 후 눌림이 잡힌다 → 상승장에서도 작동
- **하락장에서는 반대로 시가 기준이 유리할 것**
→ **레짐에 따라 기준선을 바꾸는 안**도 검토 대상.

**② "당일 고점 +10% 도달 시 매도"는 기존 기각 항목과 인접하다**
기록: *⛔ 기각 8건(재시도 금지) — 고정익절 2/3/4%(28조합 전부 적자) · **목표가 예약매도***
- 다만 **+10%는 2/3/4%와 크기가 다르고**, "미도달 시 익일 이월"은 검증된 적 없는 새 조합이다.
- **기각된 것과 동일하다고 단정하지 말 것. 단, 백테 없이 실전 투입도 하지 말 것.**
- 상한가 종가매수는 이미 **익일 시가매도 +5.976%/승률 76.5%** 로 검증돼 있다 —
  이 규칙을 바꾸면 **검증된 엣지를 건드리는 것**이므로 특히 신중해야 한다.

**③ 시간창 09:00 통일 — S02는 백테와 충돌한다**
기록: *"S02 유일 플러스 구간 진입창 **09:30~10:00**"*
오늘 실측: S01이 09:00:18~09:02:46 에 7건 매수 → **7건 전부 손실(-12,890원)**
→ 09시대가 좋다는 근거가 아직 없다. 0-C 의 그림자 검증이 먼저.

**④ 유니버스가 여전히 병목 (0-D)**
깊이 문턱을 어떻게 정하든, 그 종목이 유니버스에 없으면 못 산다.
오늘 -13%/-17% 종목은 전부 **거래대금 100억 미만**이라 고저폭30 밖이었다.
**순서: 유니버스 → 기준선 → 문턱 → 매도규칙.**

### 검증 순서 제안 (한 번에 하나씩)
1. **기준선** 확정 (그림자 3일 · 시가 vs 장중고점 vs 레짐연동)
2. **유니버스** 범위 (대금 문턱 · 구독 50칸 제약)
3. **깊이 문턱** -3/-6/-10 빈도·수익 실측
4. **매도규칙** 은 맨 마지막 — 검증된 익일시가매도를 대체하려면 백테 필수

---

## 0-D. 저점매수가 -13% 종목을 못 보는 진짜 이유 = **거래대금 100억 문턱** (7/31 09:58 실측)

### 관측
오늘 시가 대비 -13% 이하로 빠진 종목이 **2개 있었는데 저점매수 유니버스(고저폭30) 밖**이었다.

| 종목 | 시가대비 저점 | 반등 | 전일 고저폭 | **전일 대금** | 탈락 사유 |
|---|---|---|---|---|---|
| 동운아나텍 | **-17.79%** | +4.41% | 10.02% ✅ | **35억** | 대금 <100억 |
| 디엔에프 | **-13.64%** | +9.89% | 11.22% ✅ | **4억** | 대금 <100억 |
| 코스텍시스 | -9.76% | +10.81% | 11.86% ✅ | **10억** | 대금 <100억 |
| 제닉 | -8.70% | +2.81% | 12.18% ✅ | **77억** | 대금 <100억 |

**넷 다 고저폭 조건(≥10%)은 통과했다. 거래대금에서만 잘렸다.**

선별기 문턱 (`common_high_range_watchlist_v1.py:20-22`)
```python
RANGE_MIN_PCT = 10.0
DAILY_VALUE_MIN_EOK = 100.0     # ← 여기서 잘림
PRICE_MIN_KRW = 10_000.0
```
3조건 통과 = **43종목** → TOP30만 사용(13개는 순위로 잘림).

### ⛔ 효과 없는 해법 (하지 말 것)
**TOP30 → TOP50 확대**: 통과한 게 43개뿐이라 43까지만 늘고, 놓친 4종목은 **애초에 통과를 못 했으므로 여전히 안 잡힌다.** 순위 문제가 아니다.

### ⚠️ 대금 문턱을 낮추기 전에 반드시 볼 것 — **기존 검증과 정반대 방향이다**
- 기존 결론: *"급락주 유니버스 = **전일대금 700억~2조**"* (7/15 검증)
  → 급락 반등 전략은 **대금이 큰 쪽**이 좋다는 결론이 이미 있다. 100억→30억은 그 반대다.
- 왕복비용이 0.29~0.47%로 엣지의 70%를 먹는 구조에서 **저유동성 종목은 슬리피지가 더 나쁘다.**
  디엔에프는 전일 대금 **4억**이다(주가 10,690원 ≈ 하루 3.7만주). 호가 스프레드가 넓 것.
- 1주씩이라 체결 자체는 되겠지만 **진입·청산 가격이 나빠진다.**

### 검증 방법 (코드 수정 0 · 3일치)
저점매수 그림자를 **대금 하한을 낮춘 확장 유니버스로도 병행 관찰**(주문 0)해서,
`대금≥100억 30종목` vs `대금≥30억 확장분` 의 **비용 반영 후** 수익을 비교한다.
비교 없이 문턱만 낮추면 검증된 결론을 근거 없이 뒤집는 것이 된다.

---

## 0-C. S02 진입창 09:30 의 기회비용 측정 (친구님 지시 7/31 09:50 — "1번, 마감 후 그림자 자료로 검증")

> 발단: 친구님 *"2번도 9시부터 급반등을 잡게 만들자. 고점은 전일 종가로."*
> **코드는 안 바꾸고 그림자 자료로 먼저 답을 낸다.** 숫자 보고 결정.

### 오늘 관측된 사실 (09:46 실측)

**① 저점이 S02 창 밖에서 형성됐다**
```
09:03~09:11  저점 형성  (동운아나텍 -17.8% · 디엔에프 -13.6% · 엘티씨 -8.7%)
09:30        S02 진입창 개방 (ENTRY_START = time(9,30))
09:42        반등 +3~10% 이미 진행 완료  → 매수 기회 소멸
```

**② "전일 종가 기준"은 오늘 0종목이었다 — 채택하면 안 된다**

고저폭 30종목으로 두 기준 대조:
```
시가 기준 -3%↓ : 3종목
전일 기준 -3%↓ : 0종목      ← 전부 놓침
```
| 종목 | 갭 | 시가대비 저점 | 전일대비 저점 |
|---|---|---|---|
| 엘티씨 | +29.3% | -8.72% | **+18.05%** |
| 피에스케이홀딩스 | +24.0% | -7.21% | **+15.03%** |

**이유**: 고저폭 유니버스는 갭상승 종목이 대부분이라, 전일 종가를 기준선으로 잡으면
장중에 아무리 빠져도 여전히 플러스다. 잡히는 건 갭하락 종목뿐인데 그건 시가부터
조건을 만족해 **저점이 아닌 자리에서 사게 된다.** 의도와 정반대.

**③ 반대 증거도 있다 — 09:00 시작이 자명하게 좋은 게 아니다**
- 기존 백테 기록: *"S02 유일 플러스 구간 진입창 **09:30~10:00**"*
- 오늘 S01이 09:00:18~09:02:46 에 7건 매수 → **7건 전부 손실 (-12,890원)**

### 검증 방법 (코드 수정 0)

**자료**: `data\lowbuy_shadow\lowbuy_shadow_*.json` — 고저폭 30종목의 시가·당일저점·저점시각·현재가를 2초 간격으로 하루 종일 기록.

**계산할 것**
1. **진입 시각 비교** — 같은 종목·같은 규칙(저점 +1% 반등)으로
   `09:00~09:30 진입` vs `09:30 이후 진입` 의 이후 수익률 전수 비교
2. **기준선 비교** — 전일종가 / 당일시가 / 장중고점 세 기준으로 각각 몇 건이 잡히고
   수익이 어떤지
3. **비용 반영 필수** — 왕복 0.29~0.47%. 이거 빼기 전 숫자는 의미 없음

**판단 시점**: 3거래일(7/31·8/3·8/4) 모인 뒤. **하루 인상으로 결정하지 말 것.**

⚠️ **한계**: 그림자는 **고저폭 30종목만** 본다. S02 후보는 202종목이라 표본이 부분집합이다.
결론을 S02 전체로 일반화할 때 이 점을 명시할 것.

### 결정 전까지 하지 말 것
- S02 `ENTRY_START = time(9,30)` 변경 금지 (`strategy_02_low_buy_signal_v1.py:26`)
- S02 에 -3% 낙폭 문턱 신설 금지 — **S02에는 원래 낙폭 문턱이 없다**
  (`detect_flow_book_exhaustion` = 하락 파동 2개 비교. -3%는 S03 규칙이지 S02 규칙이 아님)

---

## 0. 먼저 확인할 것 (오늘 실전 결과)

- [ ] `data\LOG\sched_EOD_GAP_LOWBUY.log` — 매수 건수·건너뛴 사유
- [ ] `DATA\eod_gap_positions.json` — `route=LOWBUY` OPEN 건수
- [ ] `data\LOG\sched_EOD_GAP_FILLCHECK.log` (15:31) — **유령 건수**
- [ ] 브로커 로그 `[HR-MICRO] ret=0` 유무 (전용 통로 첫 가동 판정)

---

## 1. 유령 청소기 실반영 전환  ✅ **7/31 08:11 완료**

**완료** — 친구님 승인("지금 켜"). `setx EOD_GAP_FILLCHECK_APPLY YES`

**왜 장 마감 후가 아니라 지금 켰나**: 태스크가 도는 시각이 **15:31** 이라, 장 끝나고 켜면
오늘 것은 이미 지나가서 못 잡는다. 이 프로그램은 **주문 0건** 이고 매수 엔진이 멈춘 뒤(14:35)
도는 것이라 당일 거래에 영향이 없다.

**검증 (부작용 0으로 실증)** — 마침 `OPEN 0건` 이라 태스크를 지금 한 번 돌려도
`"대조할 OPEN 없음 — 종료"` 로 끝난다. 모드 표시는 조기 종료보다 **먼저** 찍힌다(137행→139행).

```
[2026-07-31 08:11:28] === 체결확인 시작 · OPEN 0건 · 실반영 ===
[2026-07-31 08:11:28] 대조할 OPEN 없음 — 종료
LastTaskResult = 0
```

**이 한 번의 실행으로 확정된 것 3가지**
1. `setx` 값을 **태스크 스케줄러가 읽는다** (모드 = `실반영`)
   ⚠단 **이미 떠 있는 셸의 자식 프로세스는 못 읽는다** — 수동 검증 시 새 셸로 할 것
2. **pythonw + `print()` 는 스케줄러에서 정상 동작**한다
   근거: `_log()` 는 `print()` 를 **먼저** 하고 파일에 쓴다(35행→39행).
   파일에 줄이 남았다 = print 가 안 죽었다. **"pythonw print 즉사" 우려는 이 경로에선 무효.**
3. 태스크 자체가 정상 기동한다 (종료코드 0 · 첫 스케줄러 실행)

**롤백**: `setx EOD_GAP_FILLCHECK_APPLY NO`

<details><summary>원래 계획 (참고용·보존)</summary>

**왜**: 매수는 `status in ("OK","TIMEOUT")` 을 성공으로 기록한다(`eod_gap_lowbuy_entry_v1.py:238`).
접수≠체결이라 미체결이면 유령 OPEN → 익일 매도 거부 → 슬롯 영구 점유.
7/30 실제 발생(475400 씨메스로보틱스).

**현재 상태**: `EOD_GAP_FILLCHECK_APPLY` 환경변수 **없음** → 기본 `NO` = 판정만, 파일 무수정.
태스크 `SAFEPLUS_EOD_GAP_FILLCHECK` 실행 인자에도 `--apply` 없음.

**실행**
```
setx EOD_GAP_FILLCHECK_APPLY YES
```

**검증** (setx는 떠 있는 프로세스에 무효 → 반드시 새 프로세스로)
```
C:\python310\python.exe -c "import os;print(os.environ.get('EOD_GAP_FILLCHECK_APPLY'))"
```
→ `YES` 나와야 함. 다음 거래일 15:31 로그에 `실반영` 표시 확인.

**롤백**: `setx EOD_GAP_FILLCHECK_APPLY NO`

</details>

---

## 2. 중복주문 차단 복구 (idempotency 고정 키)  ✅ **7/31 08:35 완료**

**완료 내역** (친구님 승인 후 장 시작 전 처리 · 백업 `backup\*_20260731_idemkey.py`)

| 파일 | 변경 |
|---|---|
| `eod_gap_lowbuy_entry_v1.py:234` | `f"eodgap_lowbuy_{today}_{code}"` — 고정 |
| `eod_gap_live_executor_v1.py:235` | 매수만 고정 `f"eodgap_buy_{today}_{code}"`, **매도는 uuid 유지** |

**매도를 일부러 안 고친 이유** — 게이트웨이는 **실패 응답도 5분간 캐시**한다(`2087행`).
매도까지 고정 키로 바꾸면 직전 실패 응답에 막혀 **09:30 강제매각이 안 나갈 수 있다.**
매도 중복은 잔고 초과분을 키움이 거부하지만, **못 파는 건 진짜 손해**다. 비대칭이 맞다.

**검증 증거**
- `py_compile` 2파일 통과
- 키 실측: 매수 2회 호출 → `eodgap_lowbuy_20260731_119850` 동일 / 매도 → 매번 다름
- 진입 판정 무결: A창 -13%·B창 -8%·창밖 거부 전부 정상
- 회귀 `tests\test_eod_gap_lowbuy_sell_v1.py` → **PASS 30 / FAIL 0** (항목 [7]이 executor 직접 검증)

**남은 한계**: 캐시 TTL 5분 → 5분 뒤 같은 키는 다시 통과. 완전 차단이 아니라
"연속 재시도 구간" 차단. 그 뒤는 잔고 대조가 받는다. 완전 차단을 원하면 별건.

<details><summary>원래 계획 (참고용·보존)</summary>

**왜**: 게이트웨이에 5분 dedup 방어가 이미 있는데(`broker_gateway_v1.py:1886-1890`),
호출부가 키에 매번 새 `uuid4()` 를 넣어 **한 번도 안 걸린다.**

**안전한 이유 (7/31 코드로 확인함)**
- 캐시 저장은 `SendOrder` 를 **실제로 쏜 뒤에만** 일어난다 (`2077행`·`2087행`)
- 미연결·TTL 초과·주문상한 거부는 전부 그 앞에서 `return` → **캐시 안 됨**
- ⇒ 고정 키로 바꿔도 **"안 쏜 주문의 정당한 재시도"는 안 막힌다.** 딱 원하는 동작.

**대상 3곳** (현역 공용코어는 이미 고정 키 `order_key` 를 쓴다 — 그 방식에 맞추는 것)

| 파일:줄 | 현재 | 제안 |
|---|---|---|
| `eod_gap_lowbuy_entry_v1.py:234` | `f"eodgap_lowbuy_{code}_{uuid.uuid4()}"` | `f"eodgap_lowbuy_{today}_{code}"` |
| `eod_gap_lowbuy_sell_v1.py:270` | `f"eodgap_lowbuy_sell_{code}_{uuid.uuid4()}"` | `f"eodgap_lowbuy_sell_{today}_{code}"` |
| `eod_gap_live_executor_v1.py:235` | `f"eodgap_{side}_{code}_{uuid.uuid4()}"` | `f"eodgap_{side}_{today}_{code}"` |

⚠️ 두 함수 모두 `today` 를 인자로 안 받는다 → 넘기거나 함수 안에서 계산 필요.
⚠️ TTL 5분이라 5분 뒤 같은 키는 다시 통과한다. 완전 차단이 아니라 **"연속 재시도 구간" 차단**.
   그 뒤는 기존 잔고 대조(`_account_holdings`)가 받는다.

**검증**: 회귀 테스트 `tests\test_eod_gap_lowbuy_sell_v1.py` 실행 + 모의(LIVE=NO)로 진입기 1회전
**롤백**: `backup\` 에 수정 전 파일 저장 후 진행

</details>

**롤백 방법** (문제 시)
```
copy /Y C:\stock_bot\RUN\backup\eod_gap_lowbuy_entry_v1_20260731_idemkey.py  C:\stock_bot\RUN\eod_gap_lowbuy_entry_v1.py
copy /Y C:\stock_bot\RUN\backup\eod_gap_live_executor_v1_20260731_idemkey.py C:\stock_bot\RUN\eod_gap_live_executor_v1.py
```

---

## 3. 잔고조회 "60초 캐시" 실작동 안 함  ✅ **7/31 11:25 완료**

**완료** — 친구님 승인("첫번째 / 진행해"). 백업 `backup\eod_gap_lowbuy_entry_v1_20260731_holdcache.py`

변경: 모듈 전역 `_HELD_CACHE` 신설 · `run_once` 지역변수 `account_held`/`held_fetched_ts` 제거 ·
조회 분기를 전역 캐시 참조로. **조회 실패(None)는 캐시하지 않는다**(실패를 60초 물고 있으면
그동안 매수가 전부 막힘 · 브로커 사망은 `is_broker_alive()` 로컬 체크에서 걸러져 TR 미발생).

검증 증거
```
py_compile              OK
held_fetched_ts 잔존    0건
캐시 동작 실측          3바퀴 → 조회 1회 (수정 전 3회) · 60초 경과 후 2회로 갱신
회귀 테스트             PASS 30 / FAIL 0
```
오늘 동작 무영향(실행 중 프로세스는 옛 코드) — 다음 기동부터 적용.

**롤백**
```
copy /Y C:\stock_bot\RUN\backup\eod_gap_lowbuy_entry_v1_20260731_holdcache.py C:\stock_bot\RUN\eod_gap_lowbuy_entry_v1.py
```

<details><summary>원래 계획 (참고용·보존)</summary>

**증상**: `eod_gap_lowbuy_entry_v1.py:166` 주석은 *"60초 캐시"* 인데,
`account_held`/`held_fetched_ts` 가 **`run_once` 지역변수**(299-300행)라 2초마다 초기화된다.
→ 캐시 수명 = 한 바퀴. 주문이 계속 실패하면 **2초마다 `opw00018` 조회**.

**위험**: 키움 과다조회 차단 전력 있음(13,344건 차단 사건).
평소엔 매수 후보 있을 때만 조회하므로 조용하지만, 실패 반복 시 폭주.

**제안**: 모듈 전역으로 올리거나 `state` 에 담아 바퀴 간 유지.
**우선순위**: 2번보다 낮음. 오늘 로그에 반복 실패가 있었는지 먼저 확인 후 결정.

---

</details>

---

</details>

---

## 5-오탐. 3분봉 날짜 가드 — **문제 아님. 목록에서 제외** (7/31 11:20 확인)

`_bars_source` 가 mtime 만 비교해 어제 봉을 쓸 수 있다고 봤으나 **오탐이었다.**

증거(오늘 실측): `돈맥_1분봉.json` 의 `prev` 봉 개수
```
09:27 → 27봉   (09:00~09:27 = 정확히 27분)
10:12 → 60봉   (상한 도달)
```
**09:00에 0봉에서 시작한다 = 날짜가 바뀌면 리셋된다.** 어제 봉을 물고 넘어오면 09:27에도 60봉이어야 한다.
→ **고칠 것 없음. 없는 문제를 고치면 그게 사고다.**

---

## 4·6·7 ✅ **7/31 13:0x 일괄 완료** (친구님 "장마감 기다릴 것 없어 지금 수정해")

### #6 포지션 파일 동시쓰기 경합 — 완료
- 원인 확정: 공용매도(executor)가 **시작 때 읽은 사본을 매도 후 통째로 저장**(`_jsave(POS, held)`)
  → 그 사이 전용 매도기가 쓴 CLOSED 가 낡은 사본에 밀려 OPEN 부활.
- 수리: 두 파일에 **공유 잠금 갱신**(`DATA\eod_gap_positions.lock` · 잠금→재읽기→자기 종목만
  수정→원자쓰기 · 3초 실패 시 fail-open). executor 의 마지막 전체 저장 **제거**.
- 검증: **2프로세스 교차 100회씩 실측 → 유실 0/200** ✅ (첫 판은 92건 유실 —
  잠긴 바이트 read 가 PermissionError 나는 도우미 버그까지 실측으로 잡아 수정)
  · 회귀 30/30 · py_compile OK
- 파일: `eod_gap_lowbuy_sell_v1.py` + `eod_gap_live_executor_v1.py`
  백업 `*_20260731_poslock.py` ×2 (쌍으로 롤백)

### #7 강제매각 실패 경보 — 완료
- FORCE 주문 실패 시: `state.force_fail` 기록 + 🚨 로그(60초 스로틀)
- 09:32 루프 종료 직전: 미완 종목 요약 🚨 로그 ("오늘 매도 주체 없음·수동 확인·익일 재시도")
- 같은 파일·같은 백업. 회귀 30/30.

### #4 금액상한 시장가 우회 — 코드 완료 · **브로커 다음 기동부터 적용**
- `broker_gateway_v1.py` [SEC-ORDERCAP]: `price=0`(시장가·최유리)이면 `_micro_snapshot`
  최근가로 **추정 금액 검사** 추가. 최근가 없으면 fail-open(수량 5주 상한이 방어).
  기존 검사 사슬(수량→지정가금액→일일건수)은 순서·조건 무변경.
- 검증: py_compile **64bit+32bit(브로커 실행환경)** OK · 판정표 6케이스 전부 기대일치
  (1주 시장가 통과 · 5주×40만 차단 · 최근가없음 통과 · 기존검사 무변경)
- 백업 `backup\broker_gateway_v1_20260731_mktcap.py`
- ⚠️ 적용: 오늘 밤 19:00 정지 후 **다음 기동부터**. 장중 브로커 무접촉.
- ⚠️ 브로커 회귀 테스트 없음 — 다음 기동 아침 `[HR-MICRO] ret=0` + 첫 주문 정상 접수로 확인할 것.

<details><summary>원래 계획 (참고용·보존)</summary>

## 4. 주문 금액상한이 시장가에 안 걸린다  ★보안

**정정**: 7/29 메모의 *"게이트웨이 금액상한 없음(미조치)"* 는 **낡았다.**
7/30에 `[SEC-ORDERCAP]` 로 들어왔다 (`broker_gateway_v1.py:2005-2029`).
현재: `BROKER_MAX_ORDER_QTY=5` · `BROKER_MAX_ORDER_KRW=1,000,000` · `BROKER_MAX_DAILY_BUY=100`

**남은 구멍**: 금액 검사가 `price > 0 and qty*price > _max_krw` 다(2018행).
**최유리·시장가 주문은 `price=0`** 이라 **금액 상한을 그냥 통과**한다.
저점매수·종가매수가 전부 `price=0, hoga_gb="06"` 이다.

→ 남는 방어는 수량 5주·하루 100건뿐. 고가 종목이면 1건만으로도 상한 의미가 없다.
**제안**: `price=0` 이면 현재가(또는 상한가)로 추정해 금액 검사. 또는 시장가 전용 수량 상한 별도.

---

## 5. 3분봉 날짜 가드 없음 (8/3 매도에 영향)

`eod_gap_lowbuy_sell_v1.py:147-157` `_bars_source` 가 **mtime만** 비교한다.
8/3 09:00~09:03 에 오늘 1분봉이 3개 안 쌓였으면 **어제 봉**으로 5일선 이탈을 판정할 수 있다.
(7/29에 고친 "CVD 날짜가드"와 같은 계열)

**제안**: 봉에 날짜가 있으면 오늘 것만, 없으면 파일 mtime이 오늘인지 확인.

---

## 6. `eod_gap_positions.json` 동시 쓰기 경합 (8/3 09:01)

공용매도(`eod_gap_live_executor_v1.py`)와 전용 매도기가 **같은 파일을 각자 read-modify-write** 한다.
겹치면 lost update → `CLOSED` 가 `OPEN` 으로 되살아나 유령이 된다.

**제안**: 전용 매도기 시작을 09:00 → 공용매도(09:01) 이후로 밀거나, 파일 락 공유.

---

## 7. 09:30 강제매도 실패 시 담당자 소멸

`LOOP_STOP = 09:32`. 그 안에 못 팔면 그날은 **아무도 안 판다**
(공용매도·PB엔진 둘 다 LOWBUY 제외). 다음 거래일 09:00에 다시 잡히긴 함.

**제안**: FORCE 실패가 N회 넘으면 경보 로그 + 바탕화면 중계판에 표시.

---

## 8. `SAFEPLUS_HIGH_RANGE_BOARD` 종료코드 1 (원인 미확인)

7/30 08:40 기동 → 종료코드 **1**.
단 `common_high_range_live_state.json` 은 **16:59까지 갱신**됨 = 8시간 정상 가동 후 끝에 죽음.
다만 `high_range_crown_shadow_20260730.csv` 는 **10:23에 멈춤**(7/27~29는 15:29까지).

→ 7/31 08:40 기동분의 종료코드와 산출물 mtime을 먼저 보고 판단.

---

## 참고: 이번 점검에서 **정상 확인**된 것 (재점검 불필요)

- 공용 09:01 매도의 LOWBUY 제외 — `eod_gap_live_executor_v1.py:960-962`
- 눌림 매도엔진의 EOD_GAP 제외 — `pullback_sell_strategy_v4_21_FIXED.py:3160`
- 최후 FLAT 무해 — S01~S05·캡틴 원장만 읽음, 계좌대조도 "오늘 순잔량>0" 조건, 시작 09:43
- 09:30 강제매각 구현됨 — `eod_gap_lowbuy_sell_v1.py:51`
- 모의는 포지션 파일 무기록 (7/29 사고 재발 방지)
- 그림자↔진입기 `"수신중"` 계약 일치
- **pythonw 무해** — 저점매수 4개 파일 전부 `print()` 가 `main()` 비-loop 경로에만 있음(실행 테스트 통과)
