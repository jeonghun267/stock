# 2026-07-24 캡틴2 런타임 안전성 보강

## 접근법
1. 오늘 로그와 메인 루프를 교차해 데이터 보드 예외가 보유종목 처리까지 건너뛰는지 확인했다.
2. 상태 저장 필드와 복구 필드를 비교해 `lane` 저장 후 미복원 문제를 찾았다.
3. 킬스위치가 런처 시작 시에만 평가되는지 확인하고 엔진·주문 어댑터 양쪽에 동적 차단을 넣었다.
4. 체결 확정 지점에서 일 매수금액·실현손익·연속손실을 계산하고 상태파일에 보존했다.
5. 오래된 상태파일은 당일 이벤트 CSV로 위험 누계를 재구축하도록 했다.
6. stale-board 폴백, 킬스위치, 일 손실 차단, lane 복구를 독립 시나리오로 검증했다.

## 하지 않은 것 + 이유
- 장중 실행 프로세스를 자동 재시작하지 않았다. 이유: 실주문 프로세스 전환은 코드 수정과 다른 운영 권한이며 사용자의 명시적 확인이 필요하다.
- 다른 매매 엔진과 수집기는 수정하지 않았다. 이유: 요청 범위는 캡틴2 전체 안전성 보강이었다.

## 재사용 규칙
시장 데이터 장애를 처리할 때는 신규 진입 경로와 기존 포지션의 위험관리 경로를 분리하라.

## 관련 파일/커맨드
- `C:\stock_bot\RUN\CAPTAIN2_MONEYFLOW_ENGINE_V1.py`
- `C:\stock_bot\RUN\hidden\SAFEPLUS_CAPTAIN2_SHADOW.cmd`
- `AST_PARSE_OK`
- `SCENARIO_TESTS_OK`
