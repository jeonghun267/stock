# CLAUDE.local.md — C:\stock_bot 금지구역·작업 규칙 (2026-08-13 친구님 지시로 제정)

이 파일은 이 저장소에서 일하는 모든 AI(Claude·GPT·Codex)에게 적용된다.
어기는 수정은 승인 없는 수정으로 간주한다. 규칙 변경은 친구님 승인으로만.

## 0. 탐색 최소화 (2026-08-26 친구님 지시 "핵심만 빨리빨리")

- 질문의 최단 증거 경로 **1개**를 먼저 정하고 그것만 판다. 막히면 대안 1개만.
- 여기저기 병렬로 뒤지지 않는다. 부수 발견은 한 줄 메모로 미루고 본류 복귀.
- 보고는 결론 먼저 3줄 이내, 상세는 보고서 파일로.

## 1. 장중 실전 파일 편집 금지 (오늘 4번 깨진 그 문제)

- **08:40~15:30 사이에는 실전 경로 파일을 편집하지 않는다.**
  (RUN\*.py 중 승인 명부·관리자 기준표에 오른 파일 전부 + 런처 .cmd/.ps1)
- 장중에 허용되는 것: 읽기 조사(복사본으로), 보고서·백테, tests\ 작성,
  긴급 대응은 친구님이 그 자리에서 명시 지시한 것만.
- 발견한 문제는 고치지 말고 **한 줄 보고 + 마감 후 안건**으로.

## 2. 전략 동결 목록 (열 때는 친구님 "OO 열어" 한마디로)

> 상태: **아직 비어 있음** — 내일(8/14)부터 실전에서 "완성" 확인되는 전략을
> 친구님 선언으로 **하나씩** 여기 올린다. 올라온 전략의 전용 파일은
> 수정 금지이며, 이후 ACL 잠금(관리자 읽기전용)까지 확대 예정.

| 전략 | 상태 | 동결 파일 |
|---|---|---|
| (없음) | | |

- 여닫는 말(친구님 지정): **"SXX 자물쇠 열어"** → 수정 → 검증 → 명부 갱신 →
  **"SXX 자물쇠 잠가"**. "열어"라는 말만으로는 열지 않는다 — 반드시 "자물쇠"가 붙어야 한다.
- 한 번에 **한 전략만** 열려 있을 수 있다.

## 3. 공용 파일 수정 시 이웃 칸 보호

`strategy_common_hold_sell_v1.py`·`ma3_common_v1.py`·`strategy_01_rotation_engine_v2.py`(공용 코어)
등 공용 파일을 고칠 때는:

- 작업 선언에 **"대상 전략 칸"을 명시**하고, 다른 전략의 프로필/분기(STRATEGY_PROFILES 의
  다른 전략 항목, 다른 strategy_id 분기)는 건드리지 않는다.
- 수정 후 diff 에서 다른 전략 칸 변경이 보이면 그 자리에서 중단·보고.
- (예정) 이웃 칸 변경 자동 검사기 — 8/14 이후 배선.

## 4. 역할 분리

- 검사·발견(누구든) 과 **수정 집행·승인 명부 관리(한 세션)** 를 분리한다.
- 같은 파일을 두 에이전트가 같은 날 만지게 됐다면, 나중 쪽이 멈추고 친구님에게 보고.
- 승인 명부(`config\live_approved_hashes_v1.json`)는 반드시
  `RUN\approval_manifest_writer_v1.py`(CAS)로만 편집한다.

## 5. 상시 금지 (기존 규칙 재확인)

- 허락 없는 실전 수정 금지 · 지시받은 것만 배선 · 트레일 매도 금지
- `.cmd` = CRLF, `.ps1`/`.cmd` = ASCII 전용 · PowerShell 로 JSON 쓰기 금지
- 검증 없는 "완료" 선언 금지 · 합성자료를 [PROD_REPLAY] 로 부르기 금지
- 브로커 BATCH_TR 대량 호출은 15:15~15:26 실전 종가매수 창과 겹치기 금지
  (8/13 실사고 — 근본 수리 전까지 대량 배치 작업 자체를 장중·장마감 직후에 돌리지 말 것)
